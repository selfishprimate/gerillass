---
title: "Loadify"
page_title: "Loadify Sass Mixin"
page_description: "Fade a page element in as the page loads with the Loadify Sass mixin, which also gives images a smooth loading effect of their own."
page_keywords: "Smooth Image Loading with Sass, Smooth Page Loading, Medium Image Loading, Bootstrap Lazy Load Images, CSS Image Loading, Sass Image Loading, Lazy Load Content, Smooth Content Loading, CSS Smooth Content Loading, Defer Image Loading"
page_image: "gerillass-loadify.jpg"
---

# Loadify

<Member name="loadify">

The **Loadify Sass mixin** is a handy tool that helps you make a page element load with a smooth fade-in effect while the page is loading.

<Hint kind="info">

**The element is visible by default, and the animation only fades it in.** The keyframes start at `opacity: 0` and `animation-fill-mode: backwards` applies that during the delay, so the fade looks the same as before while an animation that never runs leaves the content where it is. Under `prefers-reduced-motion: reduce` the mixin writes `animation: none`, and nothing else is needed.

</Hint>

</Member>

<KeepScroll />

<Hint kind="info">

**Refreshing keeps your place on this page.** The effect only runs when the page loads, so every example below asks you to refresh. This one page puts the scroll position back where it was, which the rest of the site does not do, so the demo you are looking at is the one that replays.

</Hint>

## Arguments

<Arguments of="loadify" footnote="The time value must be given in either seconds (s) or milliseconds (ms). The value must be zero or positive, and the unit is always required.">
  <Argument name="$mode" type="string">Accepts <code>init</code> at the root of your stylesheet, which writes the keyframes once. Since 4.0.0 a stylesheet that forgets it still builds: the elements simply do not fade.</Argument>
  <Argument name="$delay" type="time">Specifies the amount of time a design element must wait before it starts appearing on the page. The default value is set to <code>0.2s</code>. One time, with its unit, or a <code>var()</code>.</Argument>
  <Argument name="$duration" type="time">Sets the length of time a page element takes to load. <strong>This is the second argument. It must always come last and cannot be used on its own.</strong> The default value is <code>0.5s</code>.</Argument>
</Arguments>

## Examples

```scss
@include loadify(init);
```
```css
//CSS Output
@keyframes loadify {
  from {
    opacity: 0;
  }
}
```

<Hint kind="info">

**Keep in mind!** You are free to give the element any selector name you want.

</Hint>

<Example caption="One call on an image. With no arguments the element fades in as soon as the page paints." setup="@include loadify(init);">
```scss
.loadify-img {
  @include loadify();
}
```
```html
<figure class="highlight-figure loadify-figure example-1">
  <img src="/images/docs/01.jpg" class="loadify-img" />
</figure>
```
</Example>

<Example caption="The same call on the items of a list, which is where the effect is usually wanted." setup="@include loadify(init);" listing="<div class=&quot;parent-element&quot;>
&#32;&#32;<div class=&quot;item&quot;>01</div>
&#32;&#32;<div class=&quot;item&quot;>02</div>
&#32;&#32;<div class=&quot;item&quot;>03</div>
&#32;&#32;<div class=&quot;item&quot;>04</div>
&#32;&#32;<div class=&quot;item&quot;>05</div>
&#32;&#32;<div class=&quot;item&quot;>06</div>
&#32;&#32;<div class=&quot;item&quot;>07</div>
&#32;&#32;<div class=&quot;item&quot;>08</div>
</div>">
```scss
.parent-element {
  .item {
    @include loadify();
  }
}
```
```html
<style>
  .parent-element { display: grid; grid-template-columns: repeat(4, 1fr); gap: 10px; font: 15px/1 Inter, ui-sans-serif, system-ui, sans-serif; }
  .item { display: grid; place-items: center; height: 64px; border-radius: 8px; background: #e2e8f0; font-weight: 700; color: #0f172a; }
</style>
<div class="parent-element example-02">
  <div class="item">01</div>
  <div class="item">02</div>
  <div class="item">03</div>
  <div class="item">04</div>
  <div class="item">05</div>
  <div class="item">06</div>
  <div class="item">07</div>
  <div class="item">08</div>
</div>
```
</Example>

<Hint kind="info">

**Call `loadify(init)` once at the root**, to write the keyframes the elements animate to. Since 4.0.0 it can live in any file: the mixin no longer works by `@extend`, so the call and the initializer no longer have to see each other, and a stylesheet that leaves `init` out builds as before with no fade.

</Hint>

<Hint kind="warning">

**Changed in Gerillass 4.0.0.** Until then the element was written as `opacity: 0; visibility: hidden` and the animation was the thing that revealed it, so anything that stopped the animation left the content invisible for good. Measured in Chrome 152, Firefox 156 and Safari 26.6.2 with a later `animation: none`, which a component or a reset can easily write: the element computed to `opacity: 0` and `visibility: hidden` in all three, gone from the page and from the accessibility tree with it. It now computes to `opacity: 1` and `visible` in all three.

</Hint>

<Hint kind="danger">

**A time needs its unit, and there is one time per argument.** Both were accepted until 4.0.0 and both produced CSS that does nothing, which is why they stop the build now.

```scss
.a {
  @include loadify(0);
}

.b {
  @include loadify(0.2s 0.4s);
}
```
```text
Error: `0` is not a valid time: a time needs its unit, and a bare `0` is read as the iteration count in the `animation` shorthand, where it stops the animation from running at all. Write `0s`.
Error: `0.2s 0.4s` is not a valid delay for `loadify`, which takes one time. A second one in the `animation` shorthand makes a browser drop the declaration, so nothing fades.
```

Measured in Chrome 152, Firefox 156 and Safari 26.6.2, on the CSS each call used to write: `animation: loadify 0.5s 0 backwards` computed to an `animation-iteration-count` of **0**, so the fade never ran, and `animation: loadify 0.5s 0.2s 0.4s backwards` computed to `animation-name: none`, the whole declaration dropped. Neither browser said anything. Write `loadify(0s)` and `loadify(0.2s, 0.4s)`.

</Hint>

<Hint kind="info">

**Inside a selector the mixin writes one `animation` shorthand**: the name, the duration, the delay and `backwards`. With no arguments that is `animation: loadify 0.5s 0.2s backwards`, which is what the example above uses. Because it is the shorthand, an `animation` of your own on the same element replaces it rather than adding to it.

</Hint>

<Example caption="An argument is the delay, so the whole group waits a second before it arrives." setup="@include loadify(init);">
```scss
.parent-element {
  .item {
    @include loadify(1s);
  }
}
```
```html
<style>
  .parent-element { display: grid; grid-template-columns: repeat(4, 1fr); gap: 10px; font: 15px/1 Inter, ui-sans-serif, system-ui, sans-serif; }
  .item { display: grid; place-items: center; height: 64px; border-radius: 8px; background: #e2e8f0; font-weight: 700; color: #0f172a; }
</style>
<div class="parent-element example-03">
  <div class="item">01</div>
  <div class="item">02</div>
  <div class="item">03</div>
  <div class="item">04</div>
  <div class="item">05</div>
  <div class="item">06</div>
  <div class="item">07</div>
  <div class="item">08</div>
</div>
```
</Example>

<Example caption="A delay per item, through [Only](/docs/only), which is what turns a fade into a stagger." setup="@include loadify(init);">
```scss
.parent-element {
  .item {
    @include only(1) {
      @include loadify(0.2s);
    }
    @include only(2) {
      @include loadify(0.4s);
    }
    @include only(3) {
      @include loadify(0.6s);
    }
    @include only(4) {
      @include loadify(0.8s);
    }
    @include only(5) {
      @include loadify(0.1s);
    }
    @include only(6) {
      @include loadify(1.2s);
    }
    @include only(7) {
      @include loadify(1.4s);
    }
    @include only(8) {
      @include loadify(1.6s);
    }
  }
}
```
```html
<style>
  .parent-element { display: grid; grid-template-columns: repeat(4, 1fr); gap: 10px; font: 15px/1 Inter, ui-sans-serif, system-ui, sans-serif; }
  .item { display: grid; place-items: center; height: 64px; border-radius: 8px; background: #e2e8f0; font-weight: 700; color: #0f172a; }
</style>
<div class="parent-element example-04">
  <div class="item">01</div>
  <div class="item">02</div>
  <div class="item">03</div>
  <div class="item">04</div>
  <div class="item">05</div>
  <div class="item">06</div>
  <div class="item">07</div>
  <div class="item">08</div>
</div>
```
</Example>

<Example caption="The same stagger with an order of its own, so the boxes arrive in a sequence the markup does not have." setup="@include loadify(init);">
```scss
.parent-element {
  .item {
    @include only(1) {
      @include loadify(0.2s, 0.5s);
    }
    @include only(2) {
      @include loadify(1.2s, 3s);
    }
    @include only(3) {
      @include loadify(0.6s, 1.5s);
    }
    @include only(4) {
      @include loadify(1.6s, 4s);
    }
    @include only(5) {
      @include loadify(1s, 2.5s);
    }
    @include only(6) {
      @include loadify(0.4s, 1s);
    }
    @include only(7) {
      @include loadify(1.4s, 3.5s);
    }
    @include only(8) {
      @include loadify(0.8s, 2s);
    }
  }
}
```
```html
<style>
  .parent-element { display: grid; grid-template-columns: repeat(4, 1fr); gap: 10px; font: 15px/1 Inter, ui-sans-serif, system-ui, sans-serif; }
  .item { display: grid; place-items: center; height: 64px; border-radius: 8px; background: #e2e8f0; font-weight: 700; color: #0f172a; }
</style>
<div class="parent-element example-05">
  <div class="item">01</div>
  <div class="item">02</div>
  <div class="item">03</div>
  <div class="item">04</div>
  <div class="item">05</div>
  <div class="item">06</div>
  <div class="item">07</div>
  <div class="item">08</div>
</div>
```
</Example>

<Example caption="A hero that arrives in order: the heading first, then the sentence under it, then the button. Three calls with three delays, and nothing in the markup says which comes first.

**Refresh the page to see it.**" setup="@include loadify(init);">
```scss
.hero {
  h4 {
    @include loadify(0.1s, 0.5s);
  }

  p {
    @include loadify(0.35s, 0.5s);
  }

  span {
    @include loadify(0.6s, 0.5s);
  }
}
```
```html
<style>
  .hero { display: grid; place-items: center; gap: 14px; padding: 40px 24px; border-radius: 12px; background: #f8fafc; text-align: center; font: 15px/1.5 Inter, ui-sans-serif, system-ui, sans-serif; }
  .hero h4 { margin: 0; font-size: 26px; font-weight: 700; color: #0f172a; }
  .hero p { margin: 0; color: #475569; max-width: 34ch; }
  .hero span { padding: 10px 18px; border-radius: 999px; background: #0f172a; color: #f8fafc; font-weight: 600; }
</style>
<div class="hero">
  <h4>Every account, one place</h4>
  <p>Bring your team, your data and your tools together in an afternoon.</p>
  <span>Start for free</span>
</div>
```
</Example>

<Example caption="A dashboard's figures, each one a beat behind the last. The stagger comes from [only](/docs/only), which picks an item by its position, so adding a fifth card needs one more pair of lines rather than a class on every card.

**Refresh the page to see it.**" setup="@include loadify(init);">
```scss
.stats .stat {
  @include only(1) {
    @include loadify(0.1s, 0.45s);
  }

  @include only(2) {
    @include loadify(0.25s, 0.45s);
  }

  @include only(3) {
    @include loadify(0.4s, 0.45s);
  }

  @include only(4) {
    @include loadify(0.55s, 0.45s);
  }
}
```
```html
<style>
  .stats { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; font: 14px/1.4 Inter, ui-sans-serif, system-ui, sans-serif; }
  .stat { padding: 16px; border-radius: 10px; background: #eef2ff; }
  .stat b { display: block; font-size: 24px; color: #1e1b4b; }
  .stat small { color: #4338ca; }
</style>
<div class="stats">
  <div class="stat"><b>1,284</b><small>Sessions</small></div>
  <div class="stat"><b>96%</b><small>Uptime</small></div>
  <div class="stat"><b>318</b><small>Signups</small></div>
  <div class="stat"><b>4.8</b><small>Rating</small></div>
</div>
```
</Example>

<Example caption="A list of photographs. One call on the image and one on its caption is enough for the whole gallery, because the delay is the same for every item: they are not staggered, they simply arrive rather than appearing all at once with the layout.

**Refresh the page to see it.**" setup="@include loadify(init);">
```scss
.gallery {
  img {
    @include loadify(0.15s, 0.6s);
  }

  figcaption {
    @include loadify(0.45s, 0.6s);
  }
}
```
```html
<style>
  .gallery { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; font: 13px/1.4 Inter, ui-sans-serif, system-ui, sans-serif; }
  .gallery figure { margin: 0; }
  .gallery img { display: block; width: 100%; aspect-ratio: 4 / 3; object-fit: cover; border-radius: 8px; }
  .gallery figcaption { padding-top: 6px; color: #475569; }
</style>
<div class="gallery">
  <figure><img src="/images/backgrounds/01.jpg" alt=""><figcaption>Dune, at first light</figcaption></figure>
  <figure><img src="/images/backgrounds/02.jpg" alt=""><figcaption>The long room</figcaption></figure>
  <figure><img src="/images/backgrounds/03.jpg" alt=""><figcaption>Nine to five</figcaption></figure>
</div>
```
</Example>

## Related Links

- [CSS animation](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/animation)
- [CSS @keyframes](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@keyframes)
- [CSS prefers-reduced-motion](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@media/prefers-reduced-motion)
