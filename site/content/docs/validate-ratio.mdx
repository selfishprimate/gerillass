---
title: "Validate Ratio"
page_title: "Validate Ratio Sass Function"
page_description: "The validateRatio Sass function turns an aspect ratio into a value for the CSS aspect-ratio property, accepting 16:9 which CSS itself will not."
page_keywords: "Gerillass validateRatio, Sass validateRatio, Sass Functions, SCSS Functions, Sass Utility Functions, Gerillass Utilities"
---

# Validate Ratio

<Member name="validateRatio">

Turns an aspect ratio into a value for the CSS aspect-ratio property.

</Member>

## Arguments

<Arguments of="validateRatio">
  <Argument name="$ratio" type="string | number (unitless)">Accepts a string such as "16/9" or "16:9", a unitless number, or null for the 16/9 default.</Argument>
</Arguments>

## Examples

<Example caption="The ratio on its own, without the aspect-ratio mixin.">
```scss
.hero {
  aspect-ratio: validateRatio("16:9");
}
.avatar {
  aspect-ratio: validateRatio(1);
}
```
</Example>

## What it refuses

Arguments are checked, so a wrong value stops the build with a message instead of quietly producing the wrong CSS.

```scss
.hero {
  aspect-ratio: validateRatio(16 9);
}
```
```text
Error: `16 9` is not a valid ratio. Pass a string like `"16/9"` or `"16:9"`, a unitless number like `1.77`, or no argument at all for the 16/9 default. You passed a list.
```

## Related Links

- [Aspect Ratio](/docs/aspect-ratio)
- [CSS ratio](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/ratio)
- [CSS aspect-ratio](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/aspect-ratio)
