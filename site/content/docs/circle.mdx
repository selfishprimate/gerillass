---
title: "Circle"
page_title: "Circle Sass Mixin"
page_description: "The Circle Sass mixin helps you create a perfect circle shape in CSS and Sass."
page_keywords: "Gerillass circle, Circle Sass mixin, Sass circle, SCSS circle, CSS border-radius, CSS width"
page_image: "gerillass-circle.jpg"
---

# Circle

<Member name="circle">

The **Circle** Sass mixin helps you easily create perfect circle shapes.

</Member>

## Arguments

<Arguments of="circle">
  <Argument name="$size" type="number (with unit)">The size of the circle, used for both the width and the height. Accepts what <a href="/docs/sizer">Sizer</a> accepts: a length or a percentage, a sizing keyword, a CSS-wide keyword, <code>var()</code> or a maths function.</Argument>
</Arguments>

## Examples

<Example caption="Call the mixin inside a selector and pass a number value (with a unit) for the `$size` argument.">
```scss
.element{
  @include circle(150px);
}
```
```html
<style>
  body { background: #f1f5f9; font: 14px/1.5 Inter, ui-sans-serif, system-ui, sans-serif; color: #0f172a; }
  .card { display: flex; align-items: center; gap: 18px; padding: 18px; background: #fff; border-radius: 16px; box-shadow: 0 6px 20px rgb(15 23 42 / 0.08); }
  .element { overflow: hidden; flex: none; }
  .element img { width: 100%; height: 100%; object-fit: cover; }
  .card b { display: block; font-size: 17px; }
  .card span { color: #64748b; }
</style>
<div class="card">
  <div class="element"><img src="/images/landscape.webp" alt="A fjord"></div>
  <div>
    <b>Lysefjord</b>
    <span>A circle is a square with the corners spent.</span>
  </div>
</div>
```
</Example>

## What it refuses

A size the browser drops stops the build. Measured in Chrome 152, that is a word that is not a sizing keyword, a unitless number other than `0`, a unit that is not a length, a negative length and a length in quotes. Until 2.3.1 these compiled into a width and height the browser dropped without a word.

```scss
.element {
  @include circle(24);
}
```
```text
Error: `24` is not a valid $size for `circle`: a unitless number is not a length; add a unit, such as `24px`. A browser drops the width and height.
```

## Related Links

- [Border Radius](/docs/border-radius)
- [CSS border-radius](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-radius)
- [CSS width](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/width)
