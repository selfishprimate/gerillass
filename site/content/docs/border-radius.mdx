---
title: "Border Radius"
page_title: "Border Radius Sass Mixin"
page_description: "Round an element's corners with the Border Radius Sass mixin. Pass one value for all four corners, or the CSS shorthand to set each corner apart."
page_keywords: "Gerillass border-radius, Border Radius Sass mixin, Sass border-radius, SCSS border-radius, CSS border-radius"
page_image: "gerillass-border-radius.jpg"
---

# Border Radius

<Member name="border-radius">

The **Border Radius** Sass mixin helps you round the corners of a selected element using the [border-radius CSS property](https://developer.mozilla.org/en-US/docs/Web/CSS/border-radius). You can pass one value (with a unit) to style all the corners equally, or use the CSS shorthand to style each corner differently. See [the examples](#examples) for more.

</Member>

## Arguments

<Arguments of="border-radius">
  <Argument name="$corner" type="string">Lets you choose which corners of an element you want to style. Accepts the following values: <code>top</code>, <code>top-right</code>, <code>right</code>, <code>bottom-right</code>, <code>bottom</code>, <code>bottom-left</code>, <code>left</code>, <code>top-left</code>, <code>cross-left</code>, <code>cross-right</code>, <code>all</code>. The logical corners <code>start-start</code>, <code>start-end</code>, <code>end-start</code> and <code>end-end</code>, named block side first, and the logical edges <code>inline-start</code>, <code>inline-end</code>, <code>block-start</code> and <code>block-end</code> follow the writing direction instead.</Argument>
  <Argument name="$value" type="number (with unit)">The size of the border radius that will be applied. Accepts a length, a percentage, <code>var()</code> or a maths function. Alone, one radius for every corner can be up to four radii and a slash, as the <code>border-radius</code> property takes; after a corner name, or as one of four radii, it is one radius or two for an elliptical corner. A CSS-wide keyword works as the whole value.</Argument>
</Arguments>

## Examples

<Example caption="One value rounds all four corners, which is the card every layout starts with.">
```scss
.element {
  @include border-radius(20px);
}
```
```html
<style>
  body { background: #fafaf8; font: 14px/1.5 Inter, ui-sans-serif, system-ui, sans-serif; color: #657167; }
  .element { overflow: hidden; background: #fff; box-shadow: 0 6px 20px rgb(34 44 37 / 0.08); }
  .element img { display: block; width: 100%; height: 150px; object-fit: cover; }
  .element p { margin: 0; padding: 14px 16px; font-weight: 600; }
</style>
<figure class="element">
  <img src="/images/landscape.webp" alt="A fjord seen from a clifftop">
  <p>Lysefjord, Norway</p>
</figure>
```
</Example>

<Example caption="Two values: an edge and a radius. `top` rounds the two corners along the top, which is the shape a sheet arrives in.">
```scss
.element {
  @include border-radius(top, 40px);
}
```
```html
<style>
  body { background: #222c25; font: 14px/1.5 Inter, ui-sans-serif, system-ui, sans-serif; color: #657167; padding: 0; }
  .sheet-stage { height: 220px; display: grid; align-items: end; }
  .element { background: #fff; padding: 18px 20px 26px; }
  .element i { display: block; width: 40px; height: 4px; margin: 0 auto 16px; border-radius: 999px; background: #dee0cd; }
  .element b { display: block; font-size: 16px; }
  .element p { margin: 4px 0 0; color: #657167; }
</style>
<div class="sheet-stage">
  <div class="element">
    <i></i>
    <b>Choose a plan</b>
    <p>Rounded at the top, because that is the edge a sheet arrives from.</p>
  </div>
</div>
```
</Example>

<Example caption="The same form on the right edge, so the notice keeps its square side against the edge of the page.">
```scss
.element {
  @include border-radius(right, 40px);
}
```
```html
<style>
  body { background: #fafaf8; font: 14px/1.5 Inter, ui-sans-serif, system-ui, sans-serif; color: #657167; padding: 26px 0; }
  .element { display: flex; align-items: center; gap: 12px; width: 82%; padding: 16px 26px 16px 20px; background: #fafaf8; color: #668f80; font-weight: 600; }
  .element span { display: grid; place-items: center; width: 30px; height: 30px; border-radius: 999px; background: #668f80; color: #fff; }
</style>
<div class="element"><span>i</span>Your export is ready to download.</div>
```
</Example>

<Example caption="`cross-left` and `cross-right` take the corners diagonally, which is how an avatar tile gets its leaf shape.">
```scss
.element {
  @include border-radius(cross-left, 40px);
}
```
```html
<style>
  body { background: #fafaf8; font: 14px/1.5 Inter, ui-sans-serif, system-ui, sans-serif; color: #657167; }
  .element { display: grid; place-items: center; width: 150px; height: 150px; background: linear-gradient(140deg, #69995d, #668f80); color: #fff; font: 700 42px Inter, ui-sans-serif, system-ui, sans-serif; }
</style>
<div class="element">AM</div>
```
</Example>

<Example caption="Four values in CSS's own shorthand order, one radius per corner, and the same four separated by commas, which reads better once the values get long. Both compile to the same declaration.">
```scss
.element {
  @include border-radius(25px 50px 100px 150px);
}

.element--commas {
  @include border-radius(25px, 50px, 100px, 150px);
}
```
```html
<style>
  body { background: #fafaf8; font: 14px/1.5 Inter, ui-sans-serif, system-ui, sans-serif; color: #657167; }
  .element { display: grid; align-content: center; gap: 6px; height: 200px; padding: 0 34px; background: linear-gradient(135deg, #5bc0bb, #668f80 55%, #ed6a5a); color: #fff; }
  .element b { font: 700 24px Inter, ui-sans-serif, system-ui, sans-serif; }
  .element span { opacity: 0.85; }
</style>
<div class="element">
  <b>Four corners, four radii</b>
  <span>The shorthand order is the one CSS uses.</span>
</div>
```
</Example>

<Example caption="A second value after each radius bends the curve into an ellipse, and a photograph is where that shows.">
```scss
.element {
  @include border-radius(100px 40px, 50px 20%, 100px 30%, 150px 2rem);
}
```
```html
<style>
  body { background: #fafaf8; font: 14px/1.5 Inter, ui-sans-serif, system-ui, sans-serif; color: #657167; }
  .element { overflow: hidden; height: 200px; background: #fff; }
  .element img { display: block; width: 100%; height: 100%; object-fit: cover; }
</style>
<figure class="element"><img src="/images/landscape.webp" alt="A fjord, cut to an elliptical frame"></figure>
```
</Example>

<Example caption="`null` skips a corner, so a dropdown can keep the two corners that meet the button square.">
```scss
.element {
  @include border-radius(null, null, 100px 30%, 150px 2rem);
}
```
```html
<style>
  body { background: #fafaf8; font: 14px/1.5 Inter, ui-sans-serif, system-ui, sans-serif; color: #657167; }
  .element { width: 280px; padding: 16px 18px; background: #fff; box-shadow: 0 10px 30px rgb(34 44 37 / 0.12); }
  .element a { display: block; padding: 9px 10px; border-radius: 8px; color: #668f80; text-decoration: none; font-weight: 600; }
  .element a:hover { background: #fafaf8; }
</style>
<div class="element">
  <a href="#">Duplicate</a>
  <a href="#">Move to folder</a>
  <a href="#">Archive</a>
</div>
```
</Example>

<Example caption="**Logical corners and edges** follow the writing direction. `inline-start` rounds the corners a line begins at: the left in a left-to-right tab and the right in a right-to-left one, from the same rule.">
```scss
.tab {
  @include border-radius("inline-start", 16px);
}
```
```html
<style>
  body { background: #fafaf8; font: 14px/1.5 Inter, ui-sans-serif, system-ui, sans-serif; color: #657167; }
  .row { display: flex; gap: 4px; margin-bottom: 14px; }
  .tab { padding: 12px 22px; background: #f2f2eb; color: #657167; font-weight: 600; }
  .tab[aria-current] { background: #222c25; color: #fff; }
</style>
<div class="row" dir="ltr"><span class="tab" aria-current="page">Overview</span><span class="tab">Activity</span></div>
<div class="row" dir="rtl" lang="ar"><span class="tab" aria-current="page">نظرة عامة</span><span class="tab">النشاط</span></div>
```
</Example>

## What it refuses

A radius the browser drops stops the build. Measured in Chrome 152, `border-radius` and the corner properties drop a negative radius, a word such as `auto`, a unit that is not a length, and more radii than they take: four for the whole box, two for one corner. Until 2.3.1 these compiled.

```scss
.element {
  @include border-radius(auto);
}
```
```text
Error: `auto` is not a valid radius for `border-radius`: it is not a length. A browser drops the declaration.
```

More radii than the property takes are refused with the count, since a fifth
value drops the whole declaration rather than being ignored.

```scss
.element {
  @include border-radius(10px 20px 30px 40px 50px);
}
```
```text
Error: `10px 20px 30px 40px 50px` is not a valid radius for `border-radius`: it is 5 radii where at most 4 are meant. A browser drops the declaration.
```

And a corner it does not know names every corner and edge it does.

```scss
.element {
  @include border-radius(sideways, 10px);
}
```
```text
Error: `sideways` is not a valid corner for `border-radius`. Pass one of: top, top-right, right, bottom-right, bottom, bottom-left, left, top-left, cross-left, cross-right, all, start-start, start-end, end-start, end-end, inline-start, inline-end, block-start, block-end.
```

## Related Links

- [Circle](/docs/circle)
- [CSS border-radius](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-radius)
