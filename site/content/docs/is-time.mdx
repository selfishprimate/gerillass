---
title: "Is Time"
page_title: "Is Time Sass Function"
page_description: "Returns the value if it is a time in s or ms, and errors otherwise."
page_keywords: "Gerillass isTime, Sass isTime, Sass Functions, SCSS Functions, Sass Utility Functions, Gerillass Utilities"
---

# Is Time

<Member name="isTime">

Returns the value if it is a time in s or ms, and errors otherwise.

</Member>

## Arguments

<Arguments of="isTime">
  <Argument name="$value" type="time">Accepts a duration such as 0.2s or 300ms, or 0.</Argument>
</Arguments>

## Examples

<Example caption="Validating a duration argument in a mixin of your own.">
```scss
@mixin fade($duration) {
  transition: opacity isTime($duration) ease;
}
.panel {
  @include fade(0.4s);
}
```
</Example>

## What it refuses

Arguments are checked, so a wrong value stops the build with a message instead of quietly producing the wrong CSS.

```scss
.panel {
  transition-duration: isTime(400px);
}
```
```text
Error: '400px' is not a valid time value. Time values must be specified in either seconds (s) or milliseconds (ms). Please try one of the following forms: '1s', '0.2s', or '3ms'
```

## Related Links

- [Is Number](/docs/is-number)
- [CSS time](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/time)
