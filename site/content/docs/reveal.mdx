---
title: "Reveal"
page_title: "Reveal Sass Mixin"
page_description: "The Reveal Sass mixin animates something in and out of the page: a popover, a dialog, or an element a class toggles, with the starting-style and allow-discrete pieces that each fail silently on their own."
page_keywords: "CSS starting-style, transition-behavior allow-discrete, animate display none, popover animation CSS, dialog animation CSS, CSS exit animation, Gerillass reveal"
page_image: "gerillass-loadify.jpg"
---

# Reveal

<Member name="reveal">

The **Reveal** Sass mixin animates something in and out of the page: a popover, a `<dialog>`, or a plain element a class shows and hides. It writes the pieces that make a `display: none` transition work, and each of those pieces does nothing on its own.

</Member>

<Hint kind="warning">

**New in Gerillass 4.0.0.**

</Hint>

<Hint kind="danger">

**Firefox does not run the exit.** Measured in Firefox 156 on all three shapes: the element animates in and then disappears at once, although the browser answers `true` for `CSS.supports("transition-behavior", "allow-discrete")`. Chrome 152 and Safari 26.6.2 animate both ways. What a Firefox user gets is the entry animation and an instant close, which is a smaller effect rather than a broken one.

</Hint>

## Arguments

<Arguments of="reveal" footnote="The mixin goes on the element that is shown and hidden, not on the button that toggles it.">
  <Argument name="$open" type="string"><code>popover</code>, the default, writes the open state as <code>:popover-open</code>; <code>dialog</code> writes <code>[open]</code>; a string such as <code>".is-open"</code> is the selector your own code adds. It is attached to the element with no combinator, so it has to start with <code>.</code>, <code>#</code>, <code>[</code> or <code>:</code>.</Argument>
  <Argument name="$duration" type="number">One time, <code>0.3s</code> by default. It is written into every part of the transition, the discrete ones included.</Argument>
  <Argument name="$move" type="list">Where it comes from: a direction, <code>top</code>, <code>bottom</code>, <code>left</code> or <code>right</code>, with an optional distance, as in <code>top 0.75rem</code>. <code>none</code> fades without moving.</Argument>
  <Argument name="$scale" type="number">A unitless number above zero, such as <code>0.95</code>, to grow into place. Left out, the size is not touched.</Argument>
  <Argument name="$backdrop" type="boolean"><code>true</code> by default, which writes the same three pieces again for <code>::backdrop</code>, since a dialog's backdrop is a second element with its own transition. <code>false</code> leaves it alone.</Argument>
  <Argument name="$easing" type="string">Any easing a transition takes, <code>ease</code> by default.</Argument>
</Arguments>

## Examples

The frames on this page run no script, so a demo here is driven by HTML alone, and for this mixin that leaves one shape: `popovertarget`, a button the browser wires to a popover by itself. Every one below is a real popover, so press each button twice, once to show and once to hide, and watch both directions. The other two shapes, a `<dialog>` and a class, open with a line of script and are written out under the examples instead.

<Example caption="The default, and the shortest call there is: 0.3s, fading and sliding down from three quarters of a rem above." listing="<button popovertarget=&quot;tip&quot;>Toggle the popover</button>
<div popover id=&quot;tip&quot; class=&quot;tooltip&quot;>Fades and slides, both ways.</div>">
```scss
.tooltip {
  @include reveal;
}
```
```html
<style>
  /* The popover is in the top layer and adds nothing to the page's height, so the frame is given room for it to open into. */
  body { min-height: 150px; }
  button { padding: 8px 14px; border: 0; border-radius: 8px; background: #0f172a; color: #fff; font: 600 13px Inter, ui-sans-serif, system-ui, sans-serif; }
  /* A popover is in the top layer, where its containing block is the viewport: `inset: auto` does not keep it under the button, so the place is given. */
  .tooltip { position: absolute; inset: auto; top: 60px; left: 16px; border: 0; border-radius: 10px; padding: 12px 14px; background: #0f172a; color: #f8fafc; font: 13px Inter, ui-sans-serif, system-ui, sans-serif; }
</style>
<button popovertarget="tip">Toggle the popover</button>
<div popover id="tip" class="tooltip">Fades and slides, both ways.</div>
```
</Example>

<Example caption="A longer duration, coming up from below and growing into place. `$move` and `$scale` are what change, and the two run together." listing="<button popovertarget=&quot;card&quot;>Toggle the card</button>
<div popover id=&quot;card&quot; class=&quot;card&quot;>
&#32;&#32;<b>Rises and grows</b>
&#32;&#32;<p>0.45s, up from 1rem below, from 92% of its size.</p>
</div>">
```scss
.card {
  @include reveal(popover, 0.45s, bottom 1rem, 0.92, false);
}
```
```html
<style>
  body { min-height: 190px; }
  button { padding: 8px 14px; border: 0; border-radius: 8px; background: #0f172a; color: #fff; font: 600 13px Inter, ui-sans-serif, system-ui, sans-serif; }
  .card { position: absolute; inset: auto; top: 60px; left: 16px; width: 260px; border: 0; border-radius: 12px; padding: 14px 16px; background: #5bc0bb; color: #04312f; font: 13px/1.5 Inter, ui-sans-serif, system-ui, sans-serif; }
  .card b { display: block; margin-bottom: 4px; font-size: 15px; }
  .card p { margin: 0; }
</style>
<button popovertarget="card">Toggle the card</button>
<div popover id="card" class="card">
  <b>Rises and grows</b>
  <p>0.45s, up from 1rem below, from 92% of its size.</p>
</div>
```
</Example>

<Example caption="`$easing` is the last argument and takes anything a transition does. This one overshoots, so the menu comes in from the right, goes a little past its place and settles back." listing="<button popovertarget=&quot;links&quot;>Toggle the menu</button>
<nav popover id=&quot;links&quot; class=&quot;dropdown&quot;>
&#32;&#32;<a href=&quot;#&quot;>Documentation</a>
&#32;&#32;<a href=&quot;#&quot;>Playground</a>
&#32;&#32;<a href=&quot;#&quot;>Releases</a>
</nav>">
```scss
.dropdown {
  @include reveal(popover, 0.5s, right 1.5rem, null, false, cubic-bezier(0.34, 1.56, 0.64, 1));
}
```
```html
<style>
  body { min-height: 200px; }
  button { padding: 8px 14px; border: 0; border-radius: 8px; background: #0f172a; color: #fff; font: 600 13px Inter, ui-sans-serif, system-ui, sans-serif; }
  /* No `display` on a popover: an author's `display` beats the rule a browser hides a closed one with. Written `display: grid` here, as the first draft of this demo was, the menu never left in any of the three browsers. */
  .dropdown { position: absolute; inset: auto; top: 60px; left: 16px; width: 190px; border: 0; border-radius: 12px; padding: 8px; background: #0f172a; font: 600 13px Inter, ui-sans-serif, system-ui, sans-serif; }
  .dropdown a { display: block; padding: 8px 10px; border-radius: 8px; color: #e2e8f0; text-decoration: none; }
  .dropdown a:hover { background: #1e293b; }
</style>
<button popovertarget="links">Toggle the menu</button>
<nav popover id="links" class="dropdown">
  <a href="#">Documentation</a>
  <a href="#">Playground</a>
  <a href="#">Releases</a>
</nav>
```
</Example>

<Example caption="`$backdrop` is left at its default here, so the layer behind the sheet fades in with it and out with it. The mixin animates that layer; what colour it is stays the demo's own line. Pressing the backdrop closes the sheet, which is the popover's own behaviour." listing="<button popovertarget=&quot;sheet&quot;>Open the sheet</button>
<div popover id=&quot;sheet&quot; class=&quot;sheet&quot;>
&#32;&#32;<b>A sheet from the bottom</b>
&#32;&#32;<p>The dark layer behind it is the backdrop, fading on the same clock.</p>
</div>">
```scss
.sheet {
  @include reveal(popover, 0.4s, bottom 2rem);
}
```
```html
<style>
  body { min-height: 230px; }
  button { padding: 8px 14px; border: 0; border-radius: 8px; background: #0f172a; color: #fff; font: 600 13px Inter, ui-sans-serif, system-ui, sans-serif; }
  .sheet { position: fixed; inset: auto 0 0 0; border: 0; border-radius: 14px 14px 0 0; padding: 16px 18px; background: #f8fafc; color: #0f172a; font: 13px/1.5 Inter, ui-sans-serif, system-ui, sans-serif; }
  .sheet b { display: block; margin-bottom: 4px; font-size: 15px; }
  .sheet p { margin: 0; }
  /* The mixin fades the backdrop in and out; this is what it is made of. */
  .sheet::backdrop { background: rgb(15 23 42 / 0.55); }
</style>
<button popovertarget="sheet">Open the sheet</button>
<div popover id="sheet" class="sheet">
  <b>A sheet from the bottom</b>
  <p>The dark layer behind it is the backdrop, fading on the same clock.</p>
</div>
```
</Example>

<Example caption="`none` for `$move` fades without moving, and at 0.15s that reads as a switch rather than a motion. It is the call to reach for when the thing being shown is small and should simply be there." listing="<button popovertarget=&quot;note&quot;>Toggle the note</button>
<div popover id=&quot;note&quot; class=&quot;note&quot;>Copied to the clipboard</div>">
```scss
.note {
  @include reveal(popover, 0.15s, none, null, false);
}
```
```html
<style>
  body { min-height: 140px; }
  button { padding: 8px 14px; border: 0; border-radius: 8px; background: #0f172a; color: #fff; font: 600 13px Inter, ui-sans-serif, system-ui, sans-serif; }
  .note { position: absolute; inset: auto; top: 60px; left: 16px; border: 0; border-radius: 999px; padding: 8px 14px; background: #1d4ed8; color: #eff6ff; font: 600 13px Inter, ui-sans-serif, system-ui, sans-serif; }
</style>
<button popovertarget="note">Toggle the note</button>
<div popover id="note" class="note">Copied to the clipboard</div>
```
</Example>

<Hint kind="danger">

**Do not set `display` on a popover this is on.** An author's `display` beats the rule a browser hides a closed popover with, so the element never leaves. Measured in Chrome 152, Firefox 156 and Safari 26.6.2: a popover carrying `display: grid`, which is how the menu above was first written, reported `display: grid` and `opacity: 0` after it was closed, invisible but still on the page and still taking clicks. Lay its contents out on a child instead, as that demo does now.

</Hint>

## A dialog, and your own selector

Only the popover is on this page. The other two shapes open with a line of script and these frames run none, measured on 21 September 2026: a demo's own script is blocked both by the frame's sandbox and by the site's `script-src`, and a `:target` link, the one other way to change state without script, navigates the srcdoc document and replaces it. So both are written out here, and the repository's own lab, which does run scripts, has all three side by side.

A `<dialog>` opened with `showModal()`. `dialog` writes the open state as `[open]`, which the browser adds and removes itself, and `$backdrop` is left at its default, so the layer behind the dialog fades with it rather than appearing and vanishing on its own:

```scss
.modal {
  @include reveal(dialog, 0.45s, none, 0.9);
}
```

An element your own code shows and hides with a class. The mixin writes the same three pieces against whatever selector you name, and the `display` pair is yours:

```scss
.menu {
  display: none;

  @include reveal(".is-open", 200ms, right 1rem, null, false);
}

.menu.is-open {
  display: block;
}
```

## Several on one page

They do not touch each other. The mixin writes no shared keyframes and needs no call at the root of your stylesheet, which `loadify` does, so every include is a rule of its own with its own duration and its own direction.

Measured in Chrome 152, Firefox 156 and Safari 26.6.2, with five of them on one page at durations from 0.2s to 0.6s: two opened at the same moment were each at their own point of their own animation 100ms in, 0.16 and 0.22 of the way, and opening or closing any one of them left the others exactly as they were.

<Hint kind="info">

**What does close another is HTML, not this mixin.** A `popover` closes any other `popover` that is open, and `showModal()` closes them all. That is the popover API's own behaviour and it happens with or without this mixin; what the mixin adds is that the one being closed animates out properly. If two should be open at once, write `popover="manual"` on them: in the same measurement the manual one stayed open through every other one opening and closing.

</Hint>

## What it refuses

```scss
.a {
  @include reveal(nonsense);
  @include reveal(popover, 0.3s, sideways);
  @include reveal(popover, 0.3s, top -1rem);
  @include reveal(popover, 0.3s, top, 0);
}
```
```text
Error: `nonsense` is not a valid $open for `reveal`. Pass `popover`, `dialog`, or the selector your own code adds to the element, starting with `.`, `#`, `[` or `:`, such as `".is-open"`.
Error: `sideways` is not a valid direction for `reveal`. Pass `none`, or one of: top, bottom, left, right, with an optional distance such as `top 1rem`.
Error: `-1rem` is not a valid distance for `reveal`: a length below zero does not work here. It is written into `translate`, and a browser drops the declaration.
Error: `0` is not a valid $scale for `reveal`. Pass a unitless number above zero, such as `0.95`, or nothing to leave the size alone.
```

A bare word for `$open` is the one worth explaining: the state is attached to the element with no combinator, so `reveal(nonsense)` would have compiled into `.aNonsense`, a selector that matches nothing and says nothing.

## Related Links

- [Loadify](/docs/loadify)
- [Motion Safe](/docs/motion-safe)
- [CSS @starting-style](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@starting-style)
- [CSS transition-behavior](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/transition-behavior)
- [CSS overlay](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/overlay)
