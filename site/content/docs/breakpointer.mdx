---
title: "Breakpointer"
page_title: "Breakpointer Sass Mixin"
page_description: "The Breakpointer Sass mixin is a handy little tool that shows which breakpoint you are at, so you can write your styles for that specific device."
page_keywords: "Gerillass breakpointer, Breakpointer Sass mixin, Sass breakpointer, SCSS breakpointer, CSS @media, CSS content"
page_image: "gerillass-breakpointer.jpg"
---

# Breakpointer

<Member name="breakpointer">

The **Breakpointer** Sass mixin writes the name of the breakpoint the page is at into a corner of it, so the width you are writing styles for is on screen rather than in your head. 

<Hint kind="info">

This mixin has been applied to this page only, to show you how the **Breakpointer Sass mixin** works. You can see the result in the top right corner of your screen. **Please, resize the width of your browser to see which breakpoint you're at.**

</Hint>

<Hint kind="info">

**Tip:** This mixin works best with the predefined breakpoint values in the `_map-for-breakpoints.scss` file. OK, let me put it this way. **If the breakpoint mixin is the knight, the breakpointer is its squire.**

</Hint>

</Member>

## Arguments

<Arguments of="breakpointer" footnote="You can pass a declaration block to change its appearance cosmetically.">
  <Argument name="$selector" type="string">Helps you target one specific selector. By default it targets the <code>::before</code> pseudo-element of the <code>&lt;body&gt;</code> tag.</Argument>
</Arguments>

## Examples

<Example caption="Called at the root with no arguments, it writes the current breakpoint's name into the corner of the page.">
```scss
@include breakpointer;
```
```html
<style>
  body { background: #fafaf8; font: 14px/1.6 Inter, ui-sans-serif, system-ui, sans-serif; color: #668f80; }
  p { margin: 0; max-width: 32em; color: #657167; }
</style>
<p>The label in the corner of this frame is the mixin's own output, and it is naming the frame's width rather than the window's. Drag the divider, or open the page on a phone, and it changes.</p>
```
</Example>

<Example caption="A declaration block dresses the label: the corner it sits in, its colours, its type.">
```scss
@include breakpointer {
  position: fixed;
  top: 0;
  right: 0;
  color: white;
  padding: 5px 10px;
  z-index: 10;
}
```
```html
<style>
  body { background: #222c25; font: 14px/1.6 Inter, ui-sans-serif, system-ui, sans-serif; color: #f2f2eb; }
  p { margin: 0; max-width: 32em; color: #657167; }
</style>
<p>The same label, dressed: pinned to the top right, on a dark pill, in the corner of whatever it is watching.</p>
```
</Example>

<Example caption="Called inside a selector, the label lands on that element instead of the page.">
```scss
.element {
  @include breakpointer();
}
```
</Example>

<Hint kind="info">

**Important:** This mixin is for development only, so don't forget to comment out or remove the code before you go to production.

</Hint>

## Related Links

- [Breakpoint](/docs/breakpoint)
- [CSS @media](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@media)
- [CSS content](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/content)
