---
title: "Columnizer"
page_title: "Columnizer Sass Mixin"
page_description: "The Columnizer Sass mixin helps you create beautiful, responsive, card-based design layouts in CSS and Sass."
page_keywords: "Gerillass columnizer, Columnizer Sass mixin, Sass columnizer, SCSS columnizer, CSS flex, CSS flex-wrap, CSS calc"
page_image: "gerillass-columnizer.jpg"
---

# Columnizer

<Member name="columnizer">

The **Columnizer** Sass mixin lays the children of an element out in **equal columns**, as many as `$columns` says, with an optional gutter written as the container's `gap`. There are no column elements: the children are the columns, and the last row can be told to fill the width it has left. 

**Important:** The Columnizer mixin **must be applied to the parent** element to **align the children** inside it!
<Hint kind="info">

**Tip:** This mixin is very useful when you want to create **card-based design layouts**.

</Hint>

</Member>

## Arguments

<Arguments of="columnizer">
  <Argument name="$columns" type="number">How many columns the children are laid out in. A number without a unit, or <code>var()</code>, <code>env()</code> or <code>attr()</code>.</Argument>
  <Argument name="$gutter" type="number (with unit)">Sets the space between the columns and between the rows, written as the container's <code>gap</code>. A length, <code>var()</code> or a maths function. Not a percentage and not below zero; see below. <code>0</code> is written as <code>0px</code>, because the gutter is multiplied inside <code>calc()</code>.</Argument>
  <Argument name="$fill" type="boolean">Lets the orphans fill the remaining gap at the end of the list. This argument is optional and <strong>should always come last</strong>. The default value is <code>false</code>.</Argument>
</Arguments>

## Examples

```html
<div class="parent-element">
  <div class="item">01</div>
  <div class="item">02</div>
  <div class="item">03</div>
  <div class="item">04</div>
  <div class="item">05</div>
  <div class="item">06</div>
  <div class="item">07</div>
  <div class="item">08</div>
</div>
```

<Example caption="Three columns, five cards. The last row holds what is left over, each still a third of the row wide, which is the part `$fill` changes.">
```scss
.parent-element{
  @include columnizer(3);
}
```
```html
<style>
  body { background: #fafaf8; font: 14px/1.5 Inter, ui-sans-serif, system-ui, sans-serif; color: #657167; }
  .item { padding: 16px; border-radius: 14px; background: #fff; box-shadow: 0 4px 14px rgb(34 44 37 / 0.07); }
  .item b { display: block; font-size: 15px; }
  .item span { color: #657167; font-size: 13px; }
  .item i { display: block; width: 28px; height: 28px; margin-bottom: 10px; border-radius: 9px; background: #f2f2eb; color: #668f80; font: 700 13px/28px Inter, ui-sans-serif, system-ui, sans-serif; font-style: normal; text-align: center; }
</style>
<div class="parent-element">
  <div class="item"><i>1</i><b>Drafts</b><span>4 waiting</span></div>
  <div class="item"><i>2</i><b>Reviews</b><span>2 waiting</span></div>
  <div class="item"><i>3</i><b>Shipped</b><span>18 this month</span></div>
  <div class="item"><i>4</i><b>Archive</b><span>310 items</span></div>
  <div class="item"><i>5</i><b>Team</b><span>7 people</span></div>
</div>
```
</Example>

<Example caption="The same five cards with a gutter. It is written as `gap`, so it separates the rows as well as the columns.">
```scss
.parent-element{
  @include columnizer(3, 20px);
}
```
```html
<style>
  body { background: #fafaf8; font: 14px/1.5 Inter, ui-sans-serif, system-ui, sans-serif; color: #657167; }
  .item { padding: 16px; border-radius: 14px; background: #fff; box-shadow: 0 4px 14px rgb(34 44 37 / 0.07); }
  .item b { display: block; font-size: 15px; }
  .item span { color: #657167; font-size: 13px; }
  .item i { display: block; width: 28px; height: 28px; margin-bottom: 10px; border-radius: 9px; background: #f2f2eb; color: #668f80; font: 700 13px/28px Inter, ui-sans-serif, system-ui, sans-serif; font-style: normal; text-align: center; }
</style>
<div class="parent-element">
  <div class="item"><i>1</i><b>Drafts</b><span>4 waiting</span></div>
  <div class="item"><i>2</i><b>Reviews</b><span>2 waiting</span></div>
  <div class="item"><i>3</i><b>Shipped</b><span>18 this month</span></div>
  <div class="item"><i>4</i><b>Archive</b><span>310 items</span></div>
  <div class="item"><i>5</i><b>Team</b><span>7 people</span></div>
</div>
```
</Example>

<Example caption="The same call again with `true` at the end: the two cards left on the last row grow to fill it, instead of sitting a third wide with a hole beside them.">
```scss
.parent-element{
  @include columnizer(3, 20px, true);
}
```
```html
<style>
  body { background: #fafaf8; font: 14px/1.5 Inter, ui-sans-serif, system-ui, sans-serif; color: #657167; }
  .item { padding: 16px; border-radius: 14px; background: #fff; box-shadow: 0 4px 14px rgb(34 44 37 / 0.07); }
  .item b { display: block; font-size: 15px; }
  .item span { color: #657167; font-size: 13px; }
  .item i { display: block; width: 28px; height: 28px; margin-bottom: 10px; border-radius: 9px; background: #f2f2eb; color: #668f80; font: 700 13px/28px Inter, ui-sans-serif, system-ui, sans-serif; font-style: normal; text-align: center; }
</style>
<div class="parent-element">
  <div class="item"><i>1</i><b>Drafts</b><span>4 waiting</span></div>
  <div class="item"><i>2</i><b>Reviews</b><span>2 waiting</span></div>
  <div class="item"><i>3</i><b>Shipped</b><span>18 this month</span></div>
  <div class="item"><i>4</i><b>Archive</b><span>310 items</span></div>
  <div class="item"><i>5</i><b>Team</b><span>7 people</span></div>
</div>
```
</Example>

<Example caption="`$gutter` and `$fill` are both optional, and `$fill` always comes last. Three columns, no gutter, and the last row filled.">
```scss
.parent-element{
  @include columnizer(3, true);
}
```
```html
<style>
  body { background: #fafaf8; font: 14px/1.5 Inter, ui-sans-serif, system-ui, sans-serif; color: #657167; }
  .item { padding: 16px; border-radius: 14px; background: #fff; box-shadow: 0 4px 14px rgb(34 44 37 / 0.07); }
  .item b { display: block; font-size: 15px; }
  .item span { color: #657167; font-size: 13px; }
  .item i { display: block; width: 28px; height: 28px; margin-bottom: 10px; border-radius: 9px; background: #f2f2eb; color: #668f80; font: 700 13px/28px Inter, ui-sans-serif, system-ui, sans-serif; font-style: normal; text-align: center; }
</style>
<div class="parent-element">
  <div class="item"><i>1</i><b>Drafts</b><span>4 waiting</span></div>
  <div class="item"><i>2</i><b>Reviews</b><span>2 waiting</span></div>
  <div class="item"><i>3</i><b>Shipped</b><span>18 this month</span></div>
  <div class="item"><i>4</i><b>Archive</b><span>310 items</span></div>
  <div class="item"><i>5</i><b>Team</b><span>7 people</span></div>
</div>
```
</Example>

<Example caption="Two columns below large and four above it, with the last row filled at both sizes. `breakpoint` and `columnizer` compose, which is the point of this one.">
```scss
.parent-element{
  @include columnizer(2, 10px, true);
  @include breakpoint(min, large) {
    @include columnizer(4, 20px, false);
  }
}
```
```html
<style>
  body { background: #fafaf8; font: 14px/1.5 Inter, ui-sans-serif, system-ui, sans-serif; color: #657167; }
  .item { padding: 16px; border-radius: 14px; background: #fff; box-shadow: 0 4px 14px rgb(34 44 37 / 0.07); }
  .item b { display: block; font-size: 15px; }
  .item span { color: #657167; font-size: 13px; }
  .item i { display: block; width: 28px; height: 28px; margin-bottom: 10px; border-radius: 9px; background: #f2f2eb; color: #668f80; font: 700 13px/28px Inter, ui-sans-serif, system-ui, sans-serif; font-style: normal; text-align: center; }
</style>
<div class="parent-element">
  <div class="item"><i>1</i><b>Drafts</b><span>4 waiting</span></div>
  <div class="item"><i>2</i><b>Reviews</b><span>2 waiting</span></div>
  <div class="item"><i>3</i><b>Shipped</b><span>18 this month</span></div>
  <div class="item"><i>4</i><b>Archive</b><span>310 items</span></div>
  <div class="item"><i>5</i><b>Team</b><span>7 people</span></div>
</div>
```
</Example>

## Interface examples

<Example caption="A form laid out in two columns. The inputs are `width: 100%` with padding and a border. The frame below has a border-box reset of its own, but they fit without one too, measured in Chrome, Firefox and Safari, because the mixin makes everything inside the columns border-box. A `box-sizing` the page sets itself still wins.">
```scss
.form-grid {
  @include columnizer(2, 16px);
}
```
```html
<style>
  .form-grid { font: 14px/1.4 Inter, ui-sans-serif, system-ui, sans-serif; color: #2f3937; }
  .field { display: grid; gap: 6px; }
  .field span { font-weight: 500; }
  .field input {
    width: 100%; height: 36px; padding: 0 12px;
    border: 1px solid #dee0cd; border-radius: 6px; font: inherit; color: inherit;
  }
</style>
<form class="form-grid" onsubmit="return false">
  <label class="field"><span>First name</span><input placeholder="Ada"></label>
  <label class="field"><span>Last name</span><input placeholder="Lovelace"></label>
  <label class="field"><span>Email</span><input type="email" placeholder="ada@example.com"></label>
  <label class="field"><span>Phone</span><input type="tel" placeholder="+44 20 7946 0000"></label>
</form>
```
</Example>

<Example caption="The same grid in a right-to-left page. The gap has no side, so the first card sits against the right edge and every row ends flush with the left one.">
```scss
.cards {
  @include columnizer(3, 12px);
}
```
```html
<style>
  .cards { font: 14px/1.4 Inter, ui-sans-serif, system-ui, sans-serif; color: #2f3937; }
  .card { padding: 14px; border: 1px solid #dee0cd; border-radius: 8px; background: #fff; }
  .card strong { display: block; margin-bottom: 4px; }
  .card p { margin: 0; color: #657167; }
</style>
<div class="cards" dir="rtl" lang="ar">
  <div class="card"><strong>القاهرة</strong><p>٢٤ درجة</p></div>
  <div class="card"><strong>دبي</strong><p>٣٥ درجة</p></div>
  <div class="card"><strong>عمّان</strong><p>٢١ درجة</p></div>
  <div class="card"><strong>بيروت</strong><p>٢٦ درجة</p></div>
  <div class="card"><strong>تونس</strong><p>٢٣ درجة</p></div>
</div>
```
</Example>

<Example caption="The column count from a custom property, with a gutter, so one rule serves grids of different widths. Each list sets `--cols` in its own markup.">
```scss
.stats {
  @include columnizer(var(--cols), 12px);
}
```
```html
<style>
  .stats { font: 14px/1.3 Inter, ui-sans-serif, system-ui, sans-serif; color: #2f3937; margin-bottom: 12px; }
  .stat { padding: 12px; border-radius: 8px; background: #fafaf8; }
  .stat span { display: block; color: #657167; font-size: 12px; }
  .stat strong { font-size: 18px; }
</style>
<div class="stats" style="--cols: 2">
  <div class="stat"><span>Revenue</span><strong>$48.2k</strong></div>
  <div class="stat"><span>Orders</span><strong>1,204</strong></div>
</div>
<div class="stats" style="--cols: 4">
  <div class="stat"><span>Visits</span><strong>9.1k</strong></div>
  <div class="stat"><span>Signups</span><strong>312</strong></div>
  <div class="stat"><span>Churn</span><strong>2.4%</strong></div>
  <div class="stat"><span>NPS</span><strong>61</strong></div>
</div>
```
</Example>

<Example caption="A long link in one card. The columns stay equal, because a column no longer grows to fit content it cannot wrap; `overflow-wrap: anywhere` on the text lets the link break inside its own column.">
```scss
.links {
  @include columnizer(3, 12px);
}
```
```html
<style>
  .links { font: 14px/1.4 Inter, ui-sans-serif, system-ui, sans-serif; color: #2f3937; }
  .link { padding: 12px; border: 1px solid #dee0cd; border-radius: 8px; overflow-wrap: anywhere; }
  .link a { color: #668f80; }
</style>
<div class="links">
  <div class="link"><a href="#">https://example.com/documentation/getting-started/installation-with-a-package-manager</a></div>
  <div class="link"><a href="#">example.com/faq</a></div>
  <div class="link"><a href="#">example.com/blog</a></div>
</div>
```
</Example>

<Hint kind="info">

**The gutter is a `gap` since 4.0.0.** It was a right margin on the columns before, with a reset on every nth one, which put the gutter on the wrong side in a right-to-left page, left one under the last row, and lost its alignment as soon as a column was hidden or the count came from a custom property.

</Hint>

<Hint kind="warning">

**Two things to know when moving to 4.0.0.**

- A margin you put on the columns now **adds** to the gap. The old margins used to override it.
- A `gap` written after the include replaces the gutter the column widths were computed with, so rows no longer end flush. Pass the gutter to the mixin.

</Hint>

<Hint kind="info">

**Everything inside the columns is `border-box`, and your own `box-sizing` wins.** The rule is written inside `:where()`, which has no specificity, so an input with `width: 100%` and padding fits its column on a page with no reset, and a `box-sizing` you set anywhere is kept.

</Hint>

<Hint kind="info">

**A long word or a wide image keeps its column.** The columns carry `min-inline-size: 0`, since a flex item is otherwise at least as wide as its content. Content that cannot fit overflows its column instead, which `overflow-wrap: anywhere` on text or `max-width: 100%` on an image handles.

</Hint>

## What it refuses

A column count or gutter the browser drops stops the build. Measured in Chrome 152, `calc(100% / 3px)` drops the column width, and so does a gutter that is unitless, a word, or a unit that is not a length. Until 2.3.1 these compiled. A gutter of `0` used to be one of them, `(3 - 1) * 0` being a number rather than a length, and is now written as `0px`.

```scss
.grid {
  @include columnizer(3px);
}
```
```text
Error: `3px` is not a valid column count for `columnizer`: a column count has no unit. A browser drops the column width.
```

Since 4.0.0 a percentage gutter stops the build too. A column gap in `%` is a share of the container's width, but a row gap in `%` is a share of its height, which a wrapping row does not have: measured in all three browsers, the rows touched. A negative gutter is refused because `gap` cannot be negative, and the browser dropped it. Until 4.0.0 both worked through the margins. Use a length, or `vw` for a gutter that follows the viewport.

```scss
.grid {
  @include columnizer(3, 5%);
}
```
```text
Error: `5%` is not a valid gutter for `columnizer`: a row gap in % is a share of the container's height, which a wrapping row does not have, so the rows would touch. Pass a length, such as `2rem` or `3vw`.
```

## Related Links

- [Container](/docs/container)
- [Adaptive](/docs/adaptive)
- [CSS flex](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/flex)
- [CSS flex-wrap](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/flex-wrap)
- [CSS calc](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/calc)
