---
title: "Glass"
page_title: "Glass Sass Mixin"
page_description: "The Glass Sass mixin writes a frosted glass panel with backdrop-filter, the prefixed property older Safari needs and the opaque fallback a browser without support has to fall back to."
page_keywords: "CSS glassmorphism, backdrop-filter Sass, frosted glass CSS, webkit-backdrop-filter, glass card CSS, backdrop-filter fallback, Gerillass glass"
page_image: "gerillass-background-image.jpg"
---

# Glass

<Member name="glass">

The **Glass** Sass mixin writes a frosted glass panel: a tint, a blur of whatever is behind it, and the fallback a browser without `backdrop-filter` shows instead.

</Member>

<Hint kind="warning">

**New in Gerillass 4.0.0.**

</Hint>

## Arguments

<Arguments of="glass" footnote="It writes no `box-shadow`: a glass card usually wants a drop shadow too, and owning the property would stop you adding one.">
  <Argument name="$blur" type="number">How far the backdrop is blurred, <code>12px</code> by default. A length, a <code>var()</code> or a <code>calc()</code>. A percentage is refused, since <code>blur()</code> takes a length.</Argument>
  <Argument name="$tint" type="color">The panel's own colour, <code>rgb(255 255 255 / 0.18)</code> by default. It has to have an alpha, or the blurred backdrop is covered by it.</Argument>
  <Argument name="$saturate" type="number">How much the backdrop's colour is lifted, <code>1.6</code> by default. A number, a percentage or a <code>var()</code>. <code>1</code> leaves the colours alone.</Argument>
  <Argument name="$edge" type="boolean"><code>true</code>, the default, writes a light hairline; <code>false</code> writes no border; a colour writes that border instead.</Argument>
  <Argument name="$fallback" type="color">The panel a browser without <code>backdrop-filter</code> shows. Left out, it is the tint with its alpha raised to <code>0.88</code>, so a tint only the browser knows, such as a <code>var()</code>, has to pass one.</Argument>
</Arguments>

## Examples

<Example caption="A card. The tint is white at 12% over a photograph, and `$fallback` is dark on purpose: without it the fallback would be white at 88%, which the white lettering would vanish into. Pass the fallback whenever the text is chosen for the glass rather than for the panel.">
```scss
.card {
  @include glass(20px, rgb(255 255 255 / 0.12), 160%, rgb(255 255 255 / 0.3), rgb(47 57 55 / 0.92));
}
```
```html
<style>
  body { min-height: 270px; display: grid; place-items: center; background: radial-gradient(circle at 15% 20%, #5bc0bb 0 22%, transparent 23%), radial-gradient(circle at 80% 15%, #668f80 0 26%, transparent 27%), radial-gradient(circle at 70% 85%, #69995d 0 24%, transparent 25%), linear-gradient(135deg, #2f3937, #668f80 45%, #2f3937); }
  .card { width: 320px; aspect-ratio: 1.586; border-radius: 18px; padding: 22px; display: grid; align-content: space-between; color: #fafaf8; font: 600 14px Inter, ui-sans-serif, system-ui, sans-serif; }
  .card__brand { display: flex; justify-content: space-between; font-size: 12px; letter-spacing: 0.14em; text-transform: uppercase; opacity: 0.8; }
  .card__number { font: 700 21px ui-monospace, SFMono-Regular, monospace; letter-spacing: 0.08em; }
  .card__row { display: flex; justify-content: space-between; font-size: 13px; }
  .card__row span:first-child { letter-spacing: 0.06em; }
</style>
<div class="card">
  <div class="card__brand"><span>Debit</span><span>Gerillass</span></div>
  <div class="card__number">4921 •••• •••• 3087</div>
  <div class="card__row"><span>A. MORGAN</span><span>08 / 29</span></div>
</div>
```
</Example>

<Example caption="A player over its own cover art, which is the other place this pattern earns its keep: the panel is dark, the edge is a colour of its own, and `$saturate` is left at `1` because the art underneath is already bright.">
```scss
.player {
  @include glass(18px, rgb(34 44 37 / 0.55), 1, rgb(191 193 164 / 0.45));
}
```
```html
<style>
  body { min-height: 260px; display: grid; place-items: center; background: conic-gradient(from 210deg at 35% 40%, #69995d, #bdc488, #5bc0bb, #668f80, #69995d); }
  .player { width: 340px; border-radius: 16px; padding: 16px; display: flex; gap: 14px; align-items: center; color: #f2f2eb; font: 14px Inter, ui-sans-serif, system-ui, sans-serif; }
  .player__art { width: 56px; height: 56px; border-radius: 10px; background: linear-gradient(140deg, #69995d, #bdc488); flex: none; }
  .player__meta { display: grid; gap: 2px; }
  .player__title { font-weight: 700; }
  .player__artist { font-size: 13px; opacity: 0.75; }
  .player__bar { margin-top: 8px; height: 4px; border-radius: 999px; background: rgb(255 255 255 / 0.25); }
  .player__bar span { display: block; width: 62%; height: 100%; border-radius: 999px; background: #f2f2eb; }
</style>
<div class="player">
  <div class="player__art"></div>
  <div class="player__meta">
    <span class="player__title">Static Bloom</span>
    <span class="player__artist">The Cascade</span>
    <div class="player__bar"><span></span></div>
  </div>
</div>
```
</Example>

<Example caption="A floating tab bar, the shape a phone app puts over a scrolling page. Light glass this time: the tint is white at 55%, the saturation is up so the colour underneath still reads through it, and the edge is white too.">
```scss
.tabs {
  @include glass(14px, rgb(255 255 255 / 0.55), 1.8, rgb(255 255 255 / 0.65));
}
```
```html
<style>
  body { min-height: 280px; margin: 0; padding: 18px 18px 0; background: #fafaf8; font: 14px/1.5 Inter, ui-sans-serif, system-ui, sans-serif; color: #668f80; }
  .feed { display: grid; gap: 10px; }
  .feed div { height: 54px; border-radius: 12px; background: linear-gradient(120deg, #5bc0bb, #dee0cd); }
  .feed div:nth-child(2) { background: linear-gradient(120deg, #eefa92, #bdc488); }
  .feed div:nth-child(3) { background: linear-gradient(120deg, #bdc488, #5bc0bb); }
  .feed div:nth-child(4) { background: linear-gradient(120deg, #bdc488, #eefa92); }
  .tabs { position: fixed; inset: auto 24px 18px; display: flex; justify-content: space-around; padding: 10px; border-radius: 999px; }
  .tabs a { display: grid; justify-items: center; gap: 3px; padding: 6px 14px; border-radius: 999px; color: #668f80; font-size: 11px; font-weight: 700; text-decoration: none; }
  .tabs a[aria-current] { background: rgb(47 57 55 / 0.12); }
  .tabs svg { width: 18px; height: 18px; fill: none; stroke: currentColor; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }
</style>
<div class="feed"><div></div><div></div><div></div><div></div></div>
<nav class="tabs">
  <a href="#" aria-current="page"><svg viewBox="0 0 24 24"><path d="M3 10.5 12 3l9 7.5"/><path d="M5 9.5V21h14V9.5"/></svg>Home</a>
  <a href="#"><svg viewBox="0 0 24 24"><circle cx="11" cy="11" r="7"/><path d="m20 20-3.5-3.5"/></svg>Search</a>
  <a href="#"><svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="7" rx="2"/><rect x="14" y="3" width="7" height="7" rx="2"/><rect x="3" y="14" width="7" height="7" rx="2"/><rect x="14" y="14" width="7" height="7" rx="2"/></svg>Browse</a>
  <a href="#"><svg viewBox="0 0 24 24"><circle cx="12" cy="8" r="4"/><path d="M4 21c0-4 3.6-6 8-6s8 2 8 6"/></svg>You</a>
</nav>
```
</Example>

<Example caption="A menu over a feed, which is where dark glass belongs: the list has to stay readable while the page keeps showing through it. `$saturate` is nearly off, because lifting the colour of a photograph behind small text is what makes it hard to read.">
```scss
.menu {
  @include glass(22px, rgb(34 44 37 / 0.62), 1.1, rgb(191 193 164 / 0.32));
}
```
```html
<style>
  body { min-height: 300px; margin: 0; padding: 16px; background: #fafaf8; font: 14px/1.5 Inter, ui-sans-serif, system-ui, sans-serif; color: #668f80; }
  .cards { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
  .cards div { height: 132px; border-radius: 14px; background: linear-gradient(150deg, #5bc0bb, #69995d); }
  .cards div:nth-child(2) { background: linear-gradient(150deg, #69995d, #668f80); }
  .cards div:nth-child(3) { background: linear-gradient(150deg, #5bc0bb, #dee0cd); }
  .cards div:nth-child(4) { background: linear-gradient(150deg, #69995d, #bdc488); }
  .menu { position: absolute; top: 44px; right: 30px; width: 210px; padding: 8px; border-radius: 16px; color: #f2f2eb; }
  .menu a { display: flex; justify-content: space-between; padding: 9px 12px; border-radius: 10px; color: inherit; font-size: 13px; font-weight: 600; text-decoration: none; }
  .menu a[aria-current] { background: rgb(242 242 235 / 0.14); }
</style>
<div class="cards"><div></div><div></div><div></div><div></div></div>
<nav class="menu">
  <a href="#" aria-current="true">Top stories<span>✓</span></a>
  <a href="#">Design</a>
  <a href="#">Engineering</a>
  <a href="#">Culture</a>
</nav>
```
</Example>

<Example caption="A widget on a wallpaper, the smallest useful piece of glass and the one most likely to be over a photograph. The blur is heavier than the panel is tall, which is what keeps a wallpaper from showing its edges through the tile.">
```scss
.widget {
  @include glass(28px, rgb(255 255 255 / 0.16), 150%, rgb(255 255 255 / 0.28), rgb(47 57 55 / 0.92));
}
```
```html
<style>
  body { min-height: 260px; display: grid; place-items: center; background: linear-gradient(200deg, #69995d, #668f80 40%, #2f3937 75%, #222c25); }
  .widget { width: 200px; padding: 18px 20px; border-radius: 22px; color: #fafaf8; font: 14px/1.4 Inter, ui-sans-serif, system-ui, sans-serif; display: grid; gap: 10px; }
  .widget__top { display: flex; justify-content: space-between; font-size: 12px; letter-spacing: 0.1em; text-transform: uppercase; opacity: 0.75; }
  .widget__temp { font-size: 44px; font-weight: 300; letter-spacing: -0.03em; }
  .widget__row { display: flex; justify-content: space-between; font-size: 12px; opacity: 0.85; }
</style>
<div class="widget">
  <div class="widget__top"><span>Istanbul</span><span>Now</span></div>
  <div class="widget__temp">21°</div>
  <div class="widget__row"><span>Clear</span><span>H 24° L 16°</span></div>
</div>
```
</Example>

## Before you use it

<Hint kind="danger">

**An ancestor that makes a backdrop root takes the blur with it.** A `transform`, a `filter`, `opacity` below 1 or `will-change` on any ancestor means nothing outside that ancestor is blurred, and because the same ancestor is a stacking context, the panel cannot rise above a later sibling either. Measured in all three browsers on a panel inside a wrapper with `transform: translateZ(0)`: the tint stayed, the blur was gone, and the decoration painted over the panel's own text. No mixin can write around this. Move the panel out of that ancestor, or take the transform off.

</Hint>

<Hint kind="info">

**The fallback is the tint, more opaque**, so a white tint gives a white panel. Text picked to sit on glass over a dark photograph can disappear into it, which is why the card above passes a dark `$fallback` of its own.

</Hint>

## What it refuses

```scss
.a {
  @include glass(4%);
  @include glass(12px, #fff);
  @include glass(12px, var(--tint));
  @include glass(12px, rgb(255 255 255 / 0.2), 10px);
}
```
```text
Error: `4%` is not a valid $blur for `glass`: a percentage does not work here. It is written into `blur()`, which takes a length.
Error: `#fff` is opaque, so `glass` would blur the backdrop and then cover it. Pass a colour with an alpha, such as `rgb(255 255 255 / 0.18)`.
Error: `glass` cannot work out a fallback from `var(--tint)`, because only the browser knows that colour. Pass `$fallback` as well: the panel a browser without `backdrop-filter` shows, opaque enough to read on, such as `rgb(255 255 255 / 0.88)`.
Error: `10px` is not a valid $saturate for `glass`. Pass a number such as `1.6`, a percentage such as `160%`, or `1` to leave the colours alone.
```

## Related Links

- [Background Image](/docs/background-image)
- [Background Pattern](/docs/background-pattern)
- [CSS backdrop-filter](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/backdrop-filter)
- [CSS @supports](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@supports)
