---
title: "Long Shadow"
page_title: "Long Shadow Sass Mixin"
page_description: "The Long Shadow Sass mixin draws the long shadow of flat design with box-shadow, stacking a copy of the element for every step of the length, in any direction and with an optional fade."
page_keywords: "CSS long shadow, flat design shadow, box-shadow layers Sass, long shadow generator, Sass box-shadow loop, CSS shadow fade, Gerillass long-shadow"
page_image: "gerillass-text-shadow.jpg"
---

# Long Shadow

<Member name="long-shadow">

The **Long Shadow** Sass mixin draws the long shadow of flat design: a solid block running off the element, at any angle, optionally fading as it goes. It is written as `box-shadow`, and the shadow is a stack of copies of the element, one per step of the length.

</Member>

<Hint kind="warning">

**New in Gerillass 4.0.0.**

</Hint>

## Arguments

<Arguments of="long-shadow" footnote="The direction is measured from the top and clockwise, as `gradient` measures one, so a diagonal shadow sits the same distance away as a straight one.">
  <Argument name="$color" type="color">The shadow's colour. Anything <code>box-shadow</code> takes: a keyword, a hex, <code>rgb()</code>, <code>currentColor</code>, <code>var()</code>, <code>color-mix()</code>, <code>oklch()</code> or a system colour.</Argument>
  <Argument name="$length" type="number">How far the shadow runs. It has to be a length this stylesheet can count, such as <code>40px</code> or <code>2.5rem</code>, since it is divided to know how many copies to write. A percentage and a unitless number are refused because a browser drops them, and <code>var()</code> and <code>calc()</code> because nothing here can count them.</Argument>
  <Argument name="$direction" type="string">One of <code>top</code>, <code>top-right</code>, <code>right</code>, <code>bottom-right</code>, <code>bottom</code>, <code>bottom-left</code>, <code>left</code>, <code>top-left</code>, or any angle, such as <code>160deg</code> or <code>0.5turn</code>. <code>bottom-right</code> by default.</Argument>
  <Argument name="$fade" type="color">The colour the shadow fades into along its length, usually <code>transparent</code>. Left out, the shadow is solid.</Argument>
  <Argument name="$step" type="number">How far apart the copies sit, in the same unit as <code>$length</code>. A fortieth of the length by default, which is 1px for a 40px shadow. Between two copies the sweep leaves a tooth the size of the step, so a step over about 4px shows on a diagonal edge, and a call stops at 500 layers.</Argument>
</Arguments>

## Examples

<Example caption="A brutalist button, and the whole of its behaviour is three calls: the shadow grows on hover and collapses under the press, and the `translate` is what keeps its far end in the same place while the button moves. Nothing animates, because the mixin writes no transition. Press it in the frame and see.">
```scss
$ink: oklch(0.21 0.04 265);

.button {
  @include long-shadow($ink, 10px, bottom-right, $step: 2px);

  &:hover {
    @include long-shadow($ink, 16px, bottom-right, $step: 2px);
    translate: -4px -4px;
  }

  &:active {
    @include long-shadow($ink, 4px, bottom-right, $step: 2px);
    translate: 4px 4px;
  }
}
```
```html
<style>
  body { min-height: 130px; display: grid; place-items: center; }
  .button { padding: 14px 24px; border: 3px solid oklch(0.21 0.04 265); border-radius: 4px; background: #fde047; color: oklch(0.21 0.04 265); font: 800 15px Inter, ui-sans-serif, system-ui, sans-serif; letter-spacing: 0.04em; text-transform: uppercase; }
</style>
<button class="button">Get started</button>
```
</Example>

## Before you use it

<Hint kind="warning">

Three things are `box-shadow`'s own behaviour rather than anything a mixin decides:

- it draws **behind** the element, so a translucent background shows every layer through itself;
- an ancestor that clips, such as a card with `overflow: hidden`, cuts the shadow at its edge;
- a transform turns the shadow with the element, so a direction is measured before the rotation, not after it.

</Hint>

## What it refuses

```scss
.a {
  @include long-shadow(nonsense, 40px);
  @include long-shadow(red, 10%);
  @include long-shadow(red, var(--length));
  @include long-shadow(red, 40px, sideways);
  @include long-shadow(red, 40px, bottom, null, 0.01px);
}
```
```text
Error: `nonsense` is not a valid $color for `long-shadow`: it is not a colour.
Error: `10%` is not a valid $length for `long-shadow`: a percentage does not work here.
Error: `var(--length)` cannot be drawn by `long-shadow`. A long shadow is a stack of copies, so the length has to be one this stylesheet can count, such as `40px` or `2.5rem`.
Error: `sideways` is not a valid $direction for `long-shadow`. Pass one of `top`, `top-right`, `right`, `bottom-right`, `bottom`, `bottom-left`, `left`, `top-left`, or an angle such as `45deg`, measured from the top and clockwise.
Error: `long-shadow` would draw 4000 layers for `40px` in steps of `0.01px`, and stops at 500. Pass a larger `$step`: measured in Safari 26.6.2, a shadow of 400 layers takes 38ms a frame to repaint and one of 1000 takes 62ms, and the stylesheet grows by about 30 bytes a layer.
```

A percentage and a unitless number are refused because browsers drop them. A `var()` length is different: `box-shadow` takes one, and it is this mixin that cannot, since it divides the length to know how many copies to write.

## Related Links

- [Text Shadow](/docs/text-shadow)
- [Gradient](/docs/gradient)
- [CSS box-shadow](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/box-shadow)
- [CSS color-mix()](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Functions/color-mix)
