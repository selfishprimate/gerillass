---
title: "Shade"
page_title: "Shade Sass Function"
page_description: "The shade Sass function mixes a colour towards black by a percentage. It is not Sass's own darken, which works in HSL and gives a different result."
page_keywords: "Gerillass shade, Sass shade, Sass Functions, SCSS Functions, Sass Utility Functions, Gerillass Utilities"
---

# Shade

<Member name="shade">

Mixes a colour towards black by a percentage.

</Member>

## Arguments

<Arguments of="shade">
  <Argument name="$color" type="color">Accepts a colour.</Argument>
  <Argument name="$percentage" type="number (percentage)">Accepts a percentage.</Argument>
</Arguments>

## Examples

<Example caption="A darker hover state derived from one brand colour.">
```scss
$brand: crimson;
.button {
  background-color: $brand;
}
.button:hover {
  background-color: shade($brand, 20%);
}
```
</Example>

## What it refuses

Arguments are checked, so a wrong value stops the build with a message instead of quietly producing the wrong CSS.

```scss
$brand: crimson;
.button:hover {
  background-color: shade(brand, 20%);
}
```
```text
Error: 'brand' is not a color value, please replace it with a valid one.
```

<Hint kind="info">

**One colour, not a list.** `isColor` answers a list, because `isColor(red blue)` is a fair question, and `shade` mixes a single colour. Until 4.0.0 a list walked past the guard and ended the build with Sass's own message, which names neither the function nor the argument.

```scss
.a {
  color: shade(red blue, 20%);
}
```
```text
Error: `red blue` is not a single colour for `shade`. Pass one colour, such as `red` or `#bada55`.
```

</Hint>

## Related Links

- [Tint](/docs/tint)
- [CSS Color Values](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/color_value)
- [CSS color-mix](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/color_value/color-mix)
