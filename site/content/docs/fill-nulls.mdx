---
title: "Fill Nulls"
page_title: "Fill Nulls Sass Function"
page_description: "The fillNulls Sass function replaces null entries in a list with 0, or drops them, so a shorthand built from optional values stays valid CSS."
page_keywords: "Gerillass fillNulls, Sass fillNulls, Sass Functions, SCSS Functions, Sass Utility Functions, Gerillass Utilities"
---

# Fill Nulls

<Member name="fillNulls">

Replaces null entries in a list with 0, or drops them.

</Member>

<Hint kind="warning">

**The second argument is `$separation` since Gerillass 4.0.0.** It was spelt `$seperation`, and that spelling still works as a keyword argument, with a warning naming the new one.

</Hint>

## Arguments

<Arguments of="fillNulls">
  <Argument name="$value" type="list">Accepts a list that may contain nulls.</Argument>
  <Argument name="$separation (comma)" type="string">Accepts <code>comma</code>, <code>space</code> or <code>slash</code>, which is the separator the returned list is built with.</Argument>
  <Argument name="$skip (false)" type="boolean">Accepts true to leave nulls in place instead of using 0. A null is left out of the CSS the list writes.</Argument>
  <Argument name="$seperation (null)" type="string">The old, misspelt name of <code>$separation</code>. Passing it still works and warns; it is a fourth argument so that nobody passes it by position.</Argument>
</Arguments>

## Examples

<Example caption="Shorthand where only some sides are given.">
```scss
.card {
  margin: fillNulls(24px null null null, space);
}
```
</Example>

## What it refuses

Arguments are checked, so a wrong value stops the build with a message instead of quietly producing the wrong CSS.

```scss
.card {
  margin: fillNulls(24px null, spaces);
}
```
```text
Error: `spaces` is not a valid $separation for `fillNulls`. Pass `comma`, `space` or `slash`.
```

## Related Links

- [Scissors](/docs/scissors)
- [CSS Values and Units](https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Values_and_units)
