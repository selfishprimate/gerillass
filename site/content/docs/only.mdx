---
title: "Only"
page_title: "Only Sass Mixin"
page_description: "Target an element by its position among siblings of the same tag with the Only Sass mixin, which writes :first-of-type, :last-of-type and :nth-of-type."
page_keywords: "Gerillass only, Only Sass mixin, Sass only, SCSS only, CSS :nth-child, CSS :first-of-type, CSS :last-of-type"
page_image: "gerillass-only.jpg"
---

# Only

<Member name="only">

The **Only** Sass mixin helps you filter elements based on their position among a group of siblings and apply your style rules to **only** those elements. This mixin uses `:first-of-type`, `:last-of-type`, and `:nth-of-type` CSS pseudo-classes.
<Hint kind="info">

You can pass a **string** value to target elements by their `id`, `class`, or `pseudo-class` selector. Or you can pass a **number** (or **multiple numbers** separated by commas) to target items by their index position in the list.

</Hint>

</Member>

## Arguments

<Arguments of="only" footnote="**Important:** Passing multiple arguments only works with numeric values, and the values must be separated by commas.">
  <Argument name="$params…" type="string">The pseudo-class, class, or id selector. Accepts <code>first</code>, <code>last</code>, <code>odd</code>, <code>even</code>, and class or id selectors.</Argument>
  <Argument name="$params…" type="number">The index number of the element in the list, a whole number counted from 1, or from the end when negative. You can also make multiple selections. A number with a unit, a fraction and <code>0</code> are refused.</Argument>
  <Argument name="$of" type="string (quoted)">The selector to count by, such as <code>".card"</code>. Without it the count is <code>:nth-of-type</code>, which counts siblings of the same tag; with it the mixin writes <code>:nth-child(… of .card)</code>, which counts only those.</Argument>
</Arguments>

## Examples

Suppose you have a group of items like the ones below, and you want to apply style changes to only some of them.

```html
<div class="list-wrapper">
  <div class="list-item">01</div>
  <div class="list-item">02</div>
  <div class="list-item">03</div>
  <div class="list-item">04</div>
  <div class="list-item">05</div>
  <div class="list-item">06</div>
</div>
```

<Example caption="Let's start with the **first** one!">
```scss
.list-wrapper{
  .list-item{
    @include only(first) {
      background-color: #4f46e5;
      color: #fff;
    }
  }
}
```
```html
<div class="list-wrapper example01">
  <div class="list-item">01</div>
  <div class="list-item">02</div>
  <div class="list-item">03</div>
  <div class="list-item">04</div>
  <div class="list-item">05</div>
  <div class="list-item">06</div>
</div>
```
</Example>

<Example caption="Now, let's try to get the **last item** in the list.">
```scss
.list-wrapper{
  .list-item{
    @include only(last) {
      background-color: #4f46e5;
      color: #fff;
    }
  }
}
```
```html
<div class="list-wrapper example02">
  <div class="list-item">01</div>
  <div class="list-item">02</div>
  <div class="list-item">03</div>
  <div class="list-item">04</div>
  <div class="list-item">05</div>
  <div class="list-item">06</div>
</div>
```
</Example>

<Example caption="Now, let's target the **second** item in the list.">
```scss
.list-wrapper{
  .list-item{
    @include only(2) {
      background-color: #4f46e5;
      color: #fff;
    }
  }
}
```
```html
<div class="list-wrapper example03">
  <div class="list-item">01</div>
  <div class="list-item">02</div>
  <div class="list-item">03</div>
  <div class="list-item">04</div>
  <div class="list-item">05</div>
  <div class="list-item">06</div>
</div>
```
</Example>

<Example caption="Let's get the elements whose position is odd (e.g. 1, 3, 5, and so on).">
```scss
.list-wrapper{
  .list-item{
    @include only(odd){
      background-color: #4f46e5;
      color: #fff;
    }
  }
}
```
```html
<div class="list-wrapper example04">
  <div class="list-item">01</div>
  <div class="list-item">02</div>
  <div class="list-item">03</div>
  <div class="list-item">04</div>
  <div class="list-item">05</div>
  <div class="list-item">06</div>
</div>
```
</Example>

<Hint kind="info">

Remember that when you make a multiple selection, the arguments you pass must be numbers separated by commas.

</Hint>

<Example caption="Now, let's try something really fancy!">
```scss
.list-wrapper{
  .list-item{
    @include only(4, 5, 6) {
      background-color: #4f46e5;
      color: #fff;
    }
  }
}
```
```html
<div class="list-wrapper example05">
  <div class="list-item">01</div>
  <div class="list-item">02</div>
  <div class="list-item">03</div>
  <div class="list-item">04</div>
  <div class="list-item">05</div>
  <div class="list-item">06</div>
</div>
```
</Example>

<Hint kind="info">

**Information:** You can pass negative values to target elements based on their position among a group of siblings, counting from the end.

</Hint>

<Example caption="Now suppose you don't know how many items will appear in the list, and **you want to target only the third item from the end**. How can you do that? It's surprisingly easy!">
```scss
.list-wrapper {
  .list-item {
    @include only(-3) {
      background-color: #4f46e5;
      color: #fff;
    }
  }
}
```
```html
<div class="list-wrapper example06">
  <div class="list-item">01</div>
  <div class="list-item">02</div>
  <div class="list-item">03</div>
  <div class="list-item">04</div>
  <div class="list-item">05</div>
  <div class="list-item">06</div>
</div>
```
</Example>

<Example caption="Now let's pass multiple negative values.">
```scss
.list-wrapper {
  .list-item {
    @include only(-1, -2, -4) {
      background-color: #4f46e5;
      color: #fff;
    }
  }
}
```
```html
<div class="list-wrapper example07">
  <div class="list-item">01</div>
  <div class="list-item">02</div>
  <div class="list-item">03</div>
  <div class="list-item">04</div>
  <div class="list-item">05</div>
  <div class="list-item">06</div>
</div>
```
</Example>

<Example caption="Now let's pass **positive and negative values together**.">
```scss
.list-wrapper {
  .list-item {
    @include only(1, -2) {
      background-color: #4f46e5;
      color: #fff;
    }
  }
}
```
```html
<div class="list-wrapper example08">
  <div class="list-item">01</div>
  <div class="list-item">02</div>
  <div class="list-item">03</div>
  <div class="list-item">04</div>
  <div class="list-item">05</div>
  <div class="list-item">06</div>
</div>
```
</Example>

## What it refuses

A position has to be a whole number. Until 2.3.1 anything numeric went into the selector as it was, and measured in Chrome 152, `:nth-of-type(10px)` and `:nth-of-type(1.5)` are invalid selectors, so the browser drops the whole rule. In a list of positions one bad value took the good ones with it: `only(1, 10deg)` styled nothing.

```scss
.list-item {
  @include only(10px) {
    color: red;
  }
}
```
```text
Error: `10px` is not a valid position for `only`: a position is a whole number such as `2`, or a multiple of n such as `3n`, and `:nth-of-type(10px)` is an invalid selector, so the browser would drop the rule. Pass a number such as `2`, or `-2` to count from the end.
```

A multiple of `n` is a position too: `only(3n)` writes `:nth-of-type(3n)` and selects every third element. An offset cannot be added, though, because Sass does the arithmetic before the mixin sees it: `only(2n+1)` arrives as `3n` and selects every third element, not the odd ones. For those, pass `odd` or `even`.

`0` is valid CSS but matches nothing, because positions count from 1.

```scss
.list-item {
  @include only(0) {
    color: red;
  }
}
```
```text
Error: `0` is not a valid position for `only`: positions count from 1, so `:nth-of-type(0)` matches nothing. Pass `1` for the first, or `-1` for the last.
```

With several arguments every one of them is a position, so a keyword or a selector goes on its own.

```scss
.list-item {
  @include only(1, "first") {
    color: red;
  }
}
```
```text
Error: `"first"` is not a valid position for `only`: several arguments are all positions, whole numbers such as `1` or `-2`. Pass a keyword such as `first`, or a selector such as `.foo`, on its own.
```

## Counting by something other than the tag

`:nth-of-type` counts an element's siblings **of the same tag**, which is
usually not the thing being counted. Put one stray element in the group and
every number moves:

```html
<div class="grid">
  <div class="note">On sale this week</div>
  <div class="card">A</div>
  <div class="card">B</div>
  <div class="card">C</div>
</div>
```

```scss
.card {
  @include only(1) {
    outline: 2px solid;
  }
}
```

That rule selects **nothing**, because the first `div` in the grid is the note,
and the note is not a card. `only(2)` selects card A. Measured in Chrome 152,
Firefox 156 and Safari 26.6.2, all three the same.

`$of` says what to count, and the numbers mean what they say again:

<Example caption="The same grid, counted by the cards. The note can move, multiply or disappear and the first card is still the first card." listing="<div class=&quot;grid&quot;>
&#32;&#32;<div class=&quot;note&quot;>On sale this week</div>
&#32;&#32;<div class=&quot;card&quot;>A</div>
&#32;&#32;<div class=&quot;card&quot;>B</div>
&#32;&#32;<div class=&quot;card&quot;>C</div>
</div>">
```scss
.card {
  @include only(1, $of: ".card") {
    outline: 2px solid #0f766e;
  }
}
```
```html
<style>
  .grid { display: flex; gap: 10px; font: 14px/1.4 Inter, ui-sans-serif, system-ui, sans-serif; }
  .note { padding: 12px; border-radius: 8px; background: #fef3c7; color: #92400e; }
  .card { flex: 1; padding: 12px; border-radius: 8px; background: #e2e8f0; text-align: center; font-weight: 700; }
</style>
<div class="grid">
  <div class="note">On sale this week</div>
  <div class="card">A</div>
  <div class="card">B</div>
  <div class="card">C</div>
</div>
```
</Example>

<Example caption="A pricing table that marks its middle plan, counted by the plans rather than by the tag, so the heading above them changes nothing." listing="<div class=&quot;plans&quot;>
&#32;&#32;<h4>Choose a plan</h4>
&#32;&#32;<div class=&quot;plan&quot;>Starter</div>
&#32;&#32;<div class=&quot;plan&quot;>Team</div>
&#32;&#32;<div class=&quot;plan&quot;>Business</div>
</div>">
```scss
.plan {
  @include only(2, $of: ".plan") {
    background: #0f172a;
    color: #f8fafc;
    transform: scale(1.06);
  }
}
```
```html
<style>
  .plans { display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; font: 14px/1.4 Inter, ui-sans-serif, system-ui, sans-serif; }
  .plans h4 { grid-column: 1 / -1; margin: 0; font-size: 15px; color: #475569; }
  .plan { padding: 18px 12px; border-radius: 10px; background: #e2e8f0; text-align: center; font-weight: 700; color: #0f172a; }
</style>
<div class="plans">
  <h4>Choose a plan</h4>
  <div class="plan">Starter</div>
  <div class="plan">Team</div>
  <div class="plan">Business</div>
</div>
```
</Example>

<Example caption="Keywords and negative positions take `$of` as well: `last` counts back from the end of the things you named, and `odd` stripes them." listing="<ul class=&quot;tasks&quot; role=&quot;list&quot;>
&#32;&#32;<li class=&quot;task done&quot;>Write the brief</li>
&#32;&#32;<li class=&quot;task&quot;>Draw the wireframes</li>
&#32;&#32;<li class=&quot;task done&quot;>Book the studio</li>
&#32;&#32;<li class=&quot;task&quot;>Send the invoice</li>
</ul>">
```scss
.task {
  @include only(odd, $of: "li.done") {
    background: #dcfce7;
  }

  @include only(last, $of: "li.done") {
    border-left: 3px solid #16a34a;
  }
}
```
```html
<style>
  .tasks { display: grid; gap: 6px; margin: 0; padding: 0; list-style: none; font: 14px/1.4 Inter, ui-sans-serif, system-ui, sans-serif; }
  .task { padding: 10px 12px; border-radius: 8px; background: #f1f5f9; color: #0f172a; }
</style>
<ul class="tasks" role="list">
  <li class="task done">Write the brief</li>
  <li class="task">Draw the wireframes</li>
  <li class="task done">Book the studio</li>
  <li class="task">Send the invoice</li>
</ul>
```
</Example>

<Hint kind="info">

**`$of` counts, it does not narrow.** `only(".featured")` is a different thing: it adds the selector to the element rather than counting anything, so passing both raises with a message saying which one you want. `:nth-child(… of S)` is in Chrome 111, Firefox 113 and Safari 9, and `CSS.supports` answers true in all three.

</Hint>

## Related Links

- [Except](/docs/except)
- [CSS :nth-child](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Selectors/:nth-child)
- [CSS :first-of-type](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Selectors/:first-of-type)
- [CSS :last-of-type](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Selectors/:last-of-type)
