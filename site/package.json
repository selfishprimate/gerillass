{
  "name": "gerillass-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "gerillass.com. Not published; see `site` in .npmignore at the repository root.",
  "scripts": {
    "dev": "vite",
    "build": "vite-react-ssg build",
    "preview": "vite preview",
    "playground-demos": "node scripts/update-playground-demos.js",
    "supporters": "node scripts/update-supporters.js",
    "build:spa": "vite build"
  },
  "//": "Versions are deliberately left where create-react-app had them. This step changes the build and nothing else, so anything that looks different afterwards is the build's doing. React, the router and the editor move in a step of their own.",
  "dependencies": {
    "codemirror": "5.65.16",
    "framer-motion": "^11.18.2",
    "react": "^18.3.1",
    "react-codemirror2": "^9.0.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.6",
    "react-syntax-highlighter": "^16.1.1",
    "react-tabs": "^6.1.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "sass": "^1.83.0",
    "vite": "^6.0.5",
    "vite-plugin-svgr": "^5.2.0",
    "vite-react-ssg": "^0.9.2"
  },
  "engines": {
    "node": ">=22"
  }
}
