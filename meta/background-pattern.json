{
  "name": "background-pattern",
  "summary": "Seventeen background patterns drawn with gradients alone: fourteen that tile, from dots and stripes to houndstooth and harlequin, and glow, vignette and mesh, which light the whole box.",
  "caveats": [
    "It replaces `background-dots` and `background-stripes`, which are removed in 4.0.0. `background-pattern(dots, ...)` and `background-pattern(stripes, ...)` take their place, with the arguments named rather than positional.",
    "A pattern is decorative. In forced colours mode a browser sets `background-image` to `none` for anything that is not a `url()`, so every pattern here disappears: never carry meaning in one.",
    "The colours default to `currentColor` mixed with transparency, so a pattern follows the text colour in either colour scheme. Pass `$color` for one to three colours, in the order the pattern layers them.",
    "Only `background-image`, `background-size`, `background-position`, `background-repeat` and, when asked for, `background-color` are written. The `background` shorthand would reset a `background-color` set before the include.",
    "A diagonal hard edge is drawn with stair steps in Firefox 156 and smoothly in Chrome 152 and Safari 26.6.2, which shows in `zigzag` and `triangles`. Feathering the stop by half a pixel was measured and did not change it.",
    "`gingham` draws its two bands at 55% of the colour, so the crossings darken on their own and an opaque colour still reads as cloth.",
    "`sunburst` and `concentric` are drawn from the middle of the box rather than from a tile, so they do not repeat: `sunburst` takes `$angle` as the width of one ray, and `concentric` takes `$size` as the gap between rings.",
    "`glow`, `vignette` and `mesh` are drawn once at the size of the box rather than tiled, so `$size` is how far the light reaches, one value or two, and `$origin` is where it comes from. `sunburst` and `concentric` read `$origin` too, since they are drawn from a point rather than tiled: from the top edge a sunburst becomes beams fanning down, and an off-centre `concentric` reads as a contour map rather than as a target. They are what premium marketing pages use behind a hero, measured from what attio.com, retool.com and raycast.com actually serve; none of them blurs anything, since a radial gradient is already soft.",
    "A mixin call writes `background-image`, so two calls on the same element replace one another rather than stacking. To fade a tiled pattern into the page, put the pattern on one element and a `vignette` in the page's colour on an element over it.",
    "`stripes` reads its line and its period apart, which no other pattern needs: `$thickness` is the line and `$size` is one line plus its gap, so the hairline hatch marketing pages use, a 1px line on a 10px tile, is `$size: 10px, $thickness: 1px`. With no `$size` the period stays twice the line, which is the even stripe `background-stripes` drew. A line as wide as the period is refused: Chrome 152, Firefox 156 and Safari 26.6.2 all clamp the last stop and paint a solid colour with no stripes at all.",
    "The default colours are `rgb(from currentColor r g b / …)` rather than `color-mix()`. Measured in Firefox 156, a `color-mix()` inside a repeating gradient is rasterised near the origin and not tiled, so a band of one or two pixels disappears over most of the box and `stripes` with no colour drew two faint lines; the same geometry with the relative form, with an rgba literal and with a plain `currentColor` drew the full field in Chrome 152, Firefox 156 and Safari 26.6.2.",
    "`triangles` takes a $direction rather than an angle. The shape is a quarter of the tile cut by a conic gradient, and only the odd multiples of 45 degrees cut it as a triangle: measured at twelve angles, 0, 90, 180 and 270 draw a square and 15, 30, 60, 75 and 120 a lopsided quadrilateral. The four names are the four rotations that work.",
    "A vignette eases from the middle out in four stops and reaches the corners by default. Two stops with a clear centre draw a visible oval halo where the darkening starts, which is what the first version did, and an ellipse that stops inside the box draws its own edge; both were rendered beside the eased version in Chrome 152, Firefox 156 and Safari 26.6.2."
  ],
  "arguments": [
    {
      "name": "$kind",
      "accepts": [
        "dots, stripes, grid, checkerboard, crosshatch, zigzag, triangles, brick, waves, houndstooth, gingham, harlequin, sunburst, concentric, glow, vignette, mesh",
        "the name in any case; anything else is refused with the list"
      ]
    },
    {
      "name": "$color",
      "accepts": [
        "one colour, or a list of two or three for the patterns that layer them",
        "a colour, currentColor, var(), color-mix() or light-dark()"
      ]
    },
    {
      "name": "$background",
      "accepts": [
        "a colour written as background-color, under the pattern",
        "null, the default, which leaves the element's own background colour"
      ]
    },
    {
      "name": "$size",
      "accepts": [
        "the tile, a length such as 2rem, or var(), env() or a calculation",
        "a percentage, a negative length and a unitless number are refused",
        "glow: how far the light reaches, one value or two, such as `90% 80%`; vignette reaches the corners unless it is given one",
        "stripes: the period, one line plus its gap, so a 1px line on a 10px tile is `$size: 10px, $thickness: 1px`"
      ]
    },
    {
      "name": "$thickness",
      "accepts": [
        "the line, dot or mortar inside the tile; the default is a tenth of the tile for dots and an eighth for the rest",
        "stripes: the width of each coloured line. With no $size the period stays twice it, which is an even stripe"
      ]
    },
    {
      "name": "$angle",
      "accepts": [
        "an angle in deg, grad, rad or turn, or a unitless number read as degrees",
        "var(), for a pattern the browser turns",
        "sunburst: how wide one ray is, such as `6deg`, the rays repeating at twice that"
      ]
    },
    {
      "name": "$stagger",
      "accepts": [
        "dots: `false` lines the dots up in a grid instead of offsetting every other row"
      ]
    },
    {
      "name": "$axis",
      "accepts": [
        "grid only: both, block for horizontal lines, or inline for vertical lines"
      ]
    },
    {
      "name": "$image",
      "accepts": [
        "an image path as a string, laid under the pattern as the last layer",
        "refused for the patterns that paint every pixel: checkerboard, and stripes with more than one colour"
      ]
    },
    {
      "name": "$origin",
      "accepts": [
        "where the pattern is drawn from, such as `50% 100%` or `top left`",
        "glow: how far the light reaches, one value or two, such as `90% 80%`; vignette reaches the corners unless it is given one",
        "a length, a percentage, or one of top, right, bottom, left and center"
      ]
    },
    {
      "name": "$direction",
      "accepts": [
        "for triangles: up, right, down, the default, or left",
        "anything else is refused with the four names"
      ]
    }
  ],
  "examples": [
    ".a { @include background-pattern(dots); }",
    ".a { @include background-pattern(stripes, $color: #2563eb, $size: 24px); }",
    ".a { @include background-pattern(grid, $color: #cbd5e1 #94a3b8, $size: 1.5rem, $thickness: 1px); }",
    ".a { @include background-pattern(checkerboard, $color: #111 #eee, $size: 12px); }",
    ".a { @include background-pattern(crosshatch, $size: 20px, $angle: 30deg); }",
    ".a { @include background-pattern(zigzag, $color: #f97316, $background: #fff7ed, $size: 28px); }",
    ".a { @include background-pattern(triangles, $color: #16a34a, $size: 28px, $direction: right); }",
    ".a { @include background-pattern(brick, $color: #b91c1c #7f1d1d, $background: #fee2e2, $size: 48px); }",
    ".a { @include background-pattern(waves, $color: #0ea5e9, $background: #e0f2fe, $size: 32px); }",
    ".a { @include background-pattern(houndstooth, $color: #111 #fff, $size: 18px); }",
    ".a { @include background-pattern(grid, $axis: block, $color: #bae6fd, $size: 1.75rem, $thickness: 1px); }",
    ".a { @include background-pattern(dots, $size: var(--tile), $color: var(--ink)); }",
    ".a { @include background-pattern(dots, $color: #94a3b8, $size: 18px, $stagger: false); }",
    ".a { @include background-pattern(dots, $color: rgb(255 255 255 / 0.5), $size: 22px, $image: \"/img/a.png\"); }",
    ".a { @include background-pattern(gingham, $color: #1d4ed8, $size: 40px); }",
    ".a { @include background-pattern(harlequin, $color: #0f766e, $background: #ccfbf1, $size: 44px); }",
    ".a { @include background-pattern(sunburst, $color: #b45309, $background: #fffbeb); }",
    ".a { @include background-pattern(sunburst, $color: rgb(29 78 216 / 0.3), $angle: 3deg); }",
    ".a { @include background-pattern(concentric, $color: #be123c, $background: #fff1f2, $size: 16px, $thickness: 2px); }",
    ".a { @include background-pattern(glow, $color: #bccbff #e6ecff, $background: #f8fafc); }",
    ".a { @include background-pattern(glow, $color: #2d4c71 #0b1220, $background: #0b1220, $origin: 0% 100%, $size: 80% 70%); }",
    ".a { @include background-pattern(vignette, $color: rgb(2 6 23 / 0.75), $background: #1e293b); }",
    ".a { @include background-pattern(mesh, $color: #f0abfc #67e8f9 #fdba74, $background: #faf5ff); }",
    ".a { @include background-pattern(mesh, $color: #7c3aed #0ea5e9 #f43f5e #22c55e, $background: #0b1220); }",
    ".a { @include background-pattern(sunburst, $color: #f59e0b, $angle: 4deg, $origin: 50% 0%); }",
    ".a { @include background-pattern(concentric, $color: #0f766e, $size: 30px, $thickness: 1px, $origin: 15% 110%); }",
    ".a { @include background-pattern(stripes, $color: rgb(15 23 42 / 0.5), $size: 10px, $thickness: 1px, $angle: 315deg); }",
    ".a { @include background-pattern(triangles, $size: 30px, $direction: up); }"
  ],
  "rejects": [
    ".a { @include background-pattern(spiral); }",
    ".a { @include background-pattern(dots, $color: notacolor); }",
    ".a { @include background-pattern(dots, $size: -2rem); }",
    ".a { @include background-pattern(dots, $size: 42); }",
    ".a { @include background-pattern(dots, $angle: 30deg); }",
    ".a { @include background-pattern(checkerboard, $thickness: 2px); }",
    ".a { @include background-pattern(stripes, $angle: 10px); }",
    ".a { @include background-pattern(grid, $axis: sideways); }",
    ".a { @include background-pattern(dots, $image: 42); }",
    ".a { @include background-pattern(dots, $tile: 2rem); }",
    ".a { @include background-pattern(dots, 2rem); }",
    ".a { @include background-pattern(dots, $thickness: -2px); }",
    ".a { @include background-pattern(stripes, $stagger: false); }",
    ".a { @include background-pattern(checkerboard, $image: \"/img/a.png\"); }",
    ".a { @include background-pattern(stripes, $color: red blue, $image: \"/img/a.png\"); }",
    ".a { @include background-pattern(sunburst, $size: 20px); }",
    ".a { @include background-pattern(concentric, $angle: 30deg); }",
    ".a { @include background-pattern(glow, $thickness: 2px); }",
    ".a { @include background-pattern(mesh, $size: 40px); }",
    ".a { @include background-pattern(glow, $origin: sideways); }",
    ".a { @include background-pattern(stripes, $size: 4px, $thickness: 10px); }",
    ".a { @include background-pattern(triangles, $direction: sideways); }",
    ".a { @include background-pattern(triangles, $angle: 30deg); }"
  ]
}
