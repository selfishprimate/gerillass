{
  "name": "sprite",
  "summary": "Sets up an element as a sprite tile: an image, a background position, or both.",
  "arguments": [
    {
      "name": "$params",
      "accepts": [
        "an image path as a string, in any format a browser loads, or a `url()`, `image-set()` or `var()`",
        "a background position",
        "both, image first, where the image is one path as a string"
      ]
    }
  ],
  "examples": [
    ".icon { @include sprite(\"/img/sprite.png\"); }",
    ".icon { @include sprite(\"/img/sprite.png\", 0 -40px); }",
    ".icon { @include sprite(0 -40px); }",
    ".icon { @include sprite(\"/img/sprite.webp\"); }",
    ".icon { @include sprite(\"/img/sprite.png?v=2\"); }",
    ".icon { @include sprite(center); }",
    ".icon { @include sprite(\"/img/sprite.png\", left 10px top 5px); }"
  ],
  "rejects": [
    ".icon { @include sprite(\"/img/a.png\" \"/img/b.png\", 0 -40px); }",
    ".icon { @include sprite(42, 0 -40px); }",
    ".icon { @include sprite(0 red); }",
    ".icon { @include sprite(\"/img/sprite.png\", 10deg); }",
    ".icon { @include sprite(\"/img/sprite.png\", 1); }",
    ".icon { @include sprite(\"/img/sprite.png\", 10px 20px 30px 40px); }",
    ".icon { @include sprite(\"/img/sprite.png\", auto); }",
    ".icon { @include sprite(42); }"
  ],
  "caveats": [
    "With one argument the mixin has to tell an image from a position, and both can be strings. Until 4.0.0 it read the last four characters and took only `.png`, `.jpg` and `.svg`, so `.webp`, `.avif`, `.gif`, `.jpeg`, an upper case `.PNG`, a path with a query such as `a.png?v=2` and a data URI were all refused. It now asks whether the value is a position, and a string that is not one is the path.",
    "A `var()` with one argument is read as the position, since that is what a single `var()` usually is. For an image in a custom property, pass it with a position: `sprite(var(--sprite), 0 0)`.",
    "A quoted position is unquoted rather than refused: `background-position: \"center\"` is dropped in Chrome 152, Firefox 156 and Safari 26.6.2, all three falling back to `0% 0%`."
  ]
}
